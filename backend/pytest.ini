[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = 
    --strict-markers
    --tb=short

markers =
    unit: Unit tests for models and services
    integration: Integration tests for blueprints
    slow: Mark tests as slow
