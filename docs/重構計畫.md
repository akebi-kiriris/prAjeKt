# PrAjeKt 重構計畫

> 最後更新：2026/06/17（Phase 10.3 主線收尾完成：backend response / OpenAPI / frontend consumer 對齊與 protected fields 文件已補齊）
> 當前焦點：**Phase 10 先收斂前後端契約、降低 schema 漂移並整理單一真相來源；Phase 11~13 再往 integration / evaluation / observability 推進**
> 開發策略：**結構重構優先 → 功能開發 → TypeScript 遷移 → 展示級部署 → AI 擴展**

---

## 📋 目錄

### A. 核心主線（建議先讀）

1. [現狀分析](#現狀分析)
2. [目標結構](#目標結構)
3. [Phase 0 - 結構重構（MVP 前置）](#phase-0---結構重構mvp-前置)
4. [Phase 1 - MVP 核心功能](#phase-1---mvp-核心功能)
5. [Phase 2 - 功能完善](#phase-2---功能完善)
6. [Phase 3 - TypeScript 遷移](#phase-3---typescript-遷移)
7. [Phase 4 - 進階功能](#phase-4---進階功能)
8. [Phase 5 - 上線與部署整合（已完成）](#phase-5---上線與部署整合已完成)

### B. 執行明細與下一步

1. [Phase 5 執行明細（Railway + Supabase + Firebase）](#phase-5-執行明細railway--supabase--firebase)
2. [CI/CD（Side Project 輕量版）](#cicdside-project-輕量版)
3. [Phase 6 - AI 產品化與資料層統一（2026/04 規劃開始）](#phase-6---ai-產品化與資料層統一202604-規劃開始)
4. [Phase 7 - AI 實用化精簡路線（2026/04）](#phase-7---ai-實用化精簡路線202604)
5. [Phase 8 - 後端抽象與交易邊界收斂（2026/05）](#phase-8---後端抽象與交易邊界收斂202605)
6. [Phase 9 - 型別註解、Docstring 與 Tool Calling（2026/06）](#phase-9---型別註解docstring-與-tool-calling202606)
7. [Phase 10 - 契約再釐清與專案熟悉（2026/06）](#phase-10---契約再釐清與專案熟悉202606)
8. [Phase 11 - Integration Test（2026/06-07）](#phase-11---integration-test202606-07)
9. [Phase 12 - Evaluation（2026/06-07）](#phase-12---evaluation202606-07)
10. [Phase 13 - Observability（2026/06-07）](#phase-13---observability202606-07)

### C. 參考附錄

1. [附錄 A - 技術棧](#附錄-a---技術棧)
2. [附錄 B - 不納入項目](#附錄-b---不納入項目)
3. [附錄 C - 歷史記錄](#附錄-c---歷史記錄)

---

## 現狀分析

### 當前專案結構（2026/04/13）

```
prajekt/
├── .github/
│   └── workflows/
│       ├── backend-tests.yml
│       └── frontend-tests.yml
├── backend/
│   ├── app.py
│   ├── blueprints/
│   ├── services/
│   ├── repositories/
│   ├── chains/                 # LangChain 鏈與工作流 + Pydantic 驗證層 (Phase 6.6+)
│   │   └── schemas.py          # Task/ToolSelection/TaskSummary/GroupSnapshot Pydantic 模型
│   ├── prompts/                # PromptTemplate 管理（Phase 6.6）
│   ├── models/
│   ├── realtime/
│   ├── migrations/
│   ├── tests/
│   └── uploads/
├── frontend/
│   ├── src/
│   │   ├── components/
│   │   ├── composables/
│   │   ├── services/           # 含 __tests__
│   │   ├── stores/             # 含 __tests__
│   │   ├── types/
│   │   ├── utils/
│   │   ├── views/
│   │   ├── styles/
│   │   └── router/
│   ├── package.json
│   └── vitest.config.ts
├── docs/
│   ├── README.md
│   ├── 進度追蹤.md
│   ├── 重構計畫.md
│   ├── architecture/
│   ├── guides/
│   ├── reference/
│   ├── runbooks/
│   └── workflows/
├── scripts/
│   └── count_loc.py
├── mcp_server.py
└── README.md
```

### 後端分層現況（已收斂）

```text
Blueprint (Route) -> Service (Use Case / Business Rule) -> Repository (Query / Persistence Helper) -> Model (ORM)
```

Phase 8 已補上 transaction helper、ORM entity builder 與共用 session helper；目前尚未導入完整 Unit of Work / AbstractRepository，待後續讀書與測試需求成熟後再評估。

### 存在的問題

| 問題 | 嚴重度 | 說明 |
|------|--------|------|
| **Timeline 區仍有超重元件** | 🟡 中等 | `TimelineDetailDialog.vue`、`TimelineViewModes.vue` 雖已拆分，但仍需持續壓住責任密度 |
| **Timelines 組件仍偏大** | 🟡 中等 | `TimelineDetailDialog.vue` 已拆出 AddTask / Share / TaskMember panels，但主檔仍承載任務詳情、風險分析、週報、RAG 規劃與 Project Files |
| **資料庫雙軌仍需收斂** | 🟡 中等 | 開發主線已切 PostgreSQL，但仍保留 SQLite 遷移/比對鏈，需逐步去風險 |
| **契約守門與 CI 前端關卡需持續補強** | 🟡 中等 | payload guardrails 已有，需補 branch protection 與前端 PR gate |
| **RAG 品質仍需評測化** | 🟡 中等 | Phase 7.3 已可用，但任務生成品質、來源排序與 prompt 需要用固定案例持續調校 |
| **完整 Unit of Work 尚未導入** | 🟢 低 | Phase 8 已先收斂 transaction 與 ORM 建構；暫不強行導入 `AbstractRepository` / UoW，避免過早抽象 |

---

## 目標結構

### 前端（Phase 6 持續優化）

```
frontend/src/
├── App.vue
├── main.ts                      # TypeScript 入口
├── style.css
├── styles/
├── components/
│   ├── Header.vue
│   ├── Sidebar.vue
│   ├── ConfirmDialog.vue
│   └── timelines/               # TimelineHeader / TimelineViewModes / TimelineDetailDialog
├── composables/
├── services/
│   ├── api.ts
│   ├── copilotService.ts
│   ├── socketService.ts
│   └── ...其他 domain services
├── stores/                      # Pinia + __tests__
├── utils/
├── types/                       # `index.ts` + domain type files（task/timeline/group/...）
├── router/
│   └── index.ts                 # TypeScript router 入口
└── views/
    └── KnowledgeBaseView.vue     # Phase 7.3 個人知識庫頁
```

### 後端（已採用 Blueprint → Service → Repository）

```
backend/
├── app.py
├── blueprints/            # 路由層
├── services/              # 商業邏輯層
├── repositories/          # 查詢、ORM entity builder、session helper（Phase 8 收斂）
├── chains/                # LangChain 鏈與工作流（Phase 6.6）
├── prompts/               # PromptTemplate 集中管理（Phase 6.6）
├── models/                # ORM 模型層
├── realtime/              # Socket 事件層
│   └── socket_events.py
├── migrations/            # schema 版本管理
├── tests/                 # pytest
└── uploads/               # 任務附件存放
```

---

## Phase 0 - 結構重構（極簡可執行版）

> **目標：建立可維護三層架構 + 讓 Timelines 不再恐怖**  
> **原則：純搬移，不優化，不新增功能。做完一步測試一步再往下。**  
> **⚠️ 計畫討論到此為止，開始寫 code。**

---

### 🔧 Step 0-1：建立 Service 層 ✅ 完成（1 天，原估 3 天）

> 所有 View 直接打 API 的程式碼，全部搬到 service 檔案。  
> **規則：不改任何邏輯，純搬移。**

- [x] `services/todoService.js` — 封裝 `/todos` 所有 API
- [x] `services/taskService.js` — 封裝 `/tasks` 所有 API（含 subtask、updateStatus）
- [x] `services/groupService.js` — 封裝 `/groups` 所有 API
- [x] `services/profileService.js` — 封裝 `/profile` 所有 API
- [x] `services/timelineService.js` — 封裝 `/timelines` 所有 API
- [x] 更新各 View 改用 service（View 邏輯不動，只換呼叫方式）
- [x] 測試所有功能正常

**Service 範例：**
```javascript
// services/todoService.js
import api from './api';

export const todoService = {
  getAll:         ()         => api.get('/todos'),
  create:         (data)     => api.post('/todos', data),
  update:         (id, data) => api.put(`/todos/${id}`, data),
  delete:         (id)       => api.delete(`/todos/${id}`),
  toggleComplete: (id)       => api.patch(`/todos/${id}/toggle`),
};
```

---

### 🔧 Step 0-2：建立 Store 層 ✅ 完成（1 天，原估 3～5 天）

> 先做 todos 和 tasks，確認 View → Store → Service 流程跑通再往下。  
> **groups、profile、timelines store 之後再補。**

- [x] `stores/todos.js` — 搬移 TodosView 的狀態與邏輯
- [x] TodosView 改用 store，測試功能正常
- [x] `stores/tasks.js` — 搬移 TasksView 的狀態與邏輯
- [x] TasksView 改用 store，測試功能正常
- [x] `stores/timelines.js` — timelines/allTasks + computed + CRUD，03/02 完成
- [x] `stores/groups.js` — groups/messages/currentGroup，03/02 完成
- [x] `stores/profile.js` — profile/stats/statCards，03/02 完成
- [x] 三個 View 更新改用對應 store（TimelinesView / GroupsView / ProfileView）

**Store 範例：**
```javascript
// stores/todos.js
import { defineStore } from 'pinia';
import { ref, computed } from 'vue';
import { todoService } from '../services/todoService';

export const useTodoStore = defineStore('todos', () => {
  const todos = ref([]);
  const loading = ref(false);

  const completedTodos = computed(() => todos.value.filter(t => t.completed));
  const pendingTodos = computed(() => todos.value.filter(t => !t.completed));

  async function fetchTodos() {
    loading.value = true;
    try {
      const res = await todoService.getAll();
      todos.value = res.data.todos;
    } finally {
      loading.value = false;
    }
  }

  async function addTodo(data) {
    const res = await todoService.create(data);
    todos.value.unshift(res.data.todo);
  }

  return { todos, loading, completedTodos, pendingTodos, fetchTodos, addTodo };
});
```

---

### ✅ Step 0-3：TimelinesView 第一層拆分（2026/02/25 完成）

> 實際結果：2569 行 → **301 行**（降幅 88%）！遠超目標。  
> 按功能區域拆分，四種視圖模式合併在 ViewModes 組件中，  
> 各子元件均可自行呼叫 service，透過 emit 通知父層刷新。

- [x] 建立 `components/timelines/` 資料夾
- [x] 抽取 `TimelineHeader.vue` — Header + Stats + View Toggle (125 行)
- [x] 抽取 `TimelineViewModes.vue` — 四種視圖模式 + 看板任務 Modal (741 行)
- [x] 抽取 `TimelineDetailDialog.vue` — 專案詳情 + 任務相關所有 Modal (405 行)
- [x] TimelinesView.vue 降到 **301 行**（含 CSS）
- [x] 編譯通過，136 模組無錯誤
- [x] **停止，進入 Phase 1**

**各元件職責：**
- `TimelineViewModes.vue`：本地管理看板篩選狀態、月曆 ref、看板任務詳情 Modal；直接呼叫 `taskService` 處理拖曳、子任務操作；emit `refresh-all` 通知父層
- `TimelineDetailDialog.vue`：本地管理所有 Modal 的開關狀態；直接呼叫 `taskService`/`timelineService` 處理任務新增、備註、留言、AI 生成、成員邀請；emit `toggle-task`/`delete-task`/`refresh-all`
- `TimelinesView.vue`：僅保留 timelines/allTasks/selectedTimeline 等核心狀態、專案 CRUD、子元件 emit 的頂層處理

> ⏸️ 視圖模式細拆（CardView、KanbanView、CalendarView）留到 Phase 1。

---

### ❌ 延後事項（不在 Phase 0 內）

以下等 Phase 0 完成且穩定後再做：

- Toast / ConfirmDialog / BaseModal 等 UI 基建
- utils/formatters.js 整理
- composables 抽取
- timelines store
- 元件細拆（Kanban、Calendar 等）

---

## Phase 1 - MVP 核心功能

> **目標：結構重構完成後，開始實作核心功能**  
> **前提：Phase 0 完成** ✅  
> **最後更新：2026/02/26**

### 1. 多人協作與權限系統 ✅ 完成（2026/02/27）

#### 後端 ✅ 完成（2026/02/26）

- [x] `timeline_users.role` 欄位已存在（0=負責人、1=協作者，無 Viewer — MVP 不需要）
- [x] `get_user_timeline_role(user_id, timeline_id)` helper（ORM）
- [x] `require_timeline_role('owner' | 'member')` decorator factory
- [x] `create_timeline` 建立後自動插入 `timeline_users`（role=0）
- [x] `get_timelines` 改 JOIN `timeline_users`，回傳每條 timeline 的 `role`
- [x] 所有 timeline 路由套上 decorator（owner/member 分級）
- [x] 新增 `DELETE /<id>/members/<member_user_id>` — 負責人踢人，不能自踢
- [x] 全面 ORM 化（移除所有 raw SQL）
- [x] 清除無用 `user_id = int(get_jwt_identity())` 宣告（共移除 6 處）

**權限對照表：**

| 操作 | Owner (0) | Collaborator (1) |
|------|:---------:|:----------------:|
| 看任務 / 留言 / 上傳檔案 | ✅ | ✅ |
| 新增任務 / 編輯備註 | ✅ | ✅ |
| 編輯專案資訊（名稱/日期）| ✅ | ❌ 403 |
| 刪除專案 | ✅ | ❌ 403 |
| 新增 / 移除成員 | ✅ | ❌ 403 |

#### 前端 🔄 部分完成（2026/02/26）

- [x] `timelineService.js` 新增 `removeMember`
- [x] `TimelineViewModes.vue` — ✏️ 🗑️ 按鈕加 `v-if="timeline.role === 0"`（List + Card 各 2 處）
- [x] `TimelineDetailDialog.vue` — 「成員管理」按鈕加 `v-if="selectedTimeline?.role === 0"`
- [x] **舊資料補填** — 跑 `backfill_timeline_users.py`，5 筆補填完成
- [x] 前端成員管理 UI 顯示現有成員列表 + 踢人按鈕（呼叫 `removeMember`）

#### Tasks 多人協作 ✅ 完成（2026/02/27）

> `task_user` 表結構與欄位齊全（role=0 負責人 / 1 協作者）。  
> 後端已統一改用 `task_user` 做所有權限判斷，舊的 `task.user_id` 邏輯已清除。

- [x] 新增 `get_user_task_role()` helper（先查 `task_user`，若無且任務屬於 timeline 則繼承 `timeline_user.role`）
- [x] 新增 `require_task_role('member' | 'owner')` decorator factory
- [x] 14 個路由全套上 decorator（見下表）
- [x] `create_task` 建立後自動 insert `task_user(role=0)` 給建立者
- [x] `assistant` 欄位已廢棄 — model 移除欄位、migration 執行完成、前端 UI/store 同步清除

**路由權限對照表：**

| 路由 | 權限 | 改動說明 |
|------|------|----------|
| `PUT /<id>` update_task | member | 移除舊 `task.user_id` 比對 |
| `DELETE /<id>` delete_task | owner | 移除舊 filter_by user_id 查詢 |
| `PATCH /<id>/toggle` | member | 移除舊 is_member 比對 |
| `GET /<id>/members` | member | 原本完全無保護 |
| `POST /<id>/members` | owner | 移除舊 `task.user_id` 比對 |
| `DELETE /<id>/members/<id>` | owner | 移除舊 `task.user_id` 比對 |
| `GET /<id>/comments` | member | 原本完全無保護 |
| `POST /<id>/comments` | member | 移除多餘的 task 查詢 |
| `GET /<id>/subtasks` | member | 原本完全無保護 |
| `POST /<id>/subtasks` | member | 移除多餘的 task 查詢 |
| `PUT /<id>/subtasks/<id>` | member | 原本完全無保護 |
| `DELETE /<id>/subtasks/<id>` | member | 原本完全無保護 |
| `PATCH /<id>/subtasks/<id>/toggle` | member | 原本完全無保護 |
| `PATCH /<id>/status` | member | 移除舊 is_member 比對 |

---

### 2. 任務留言與檔案上傳 ✅ 完成（2026/03/01~02）

- [x] 後端 `tasks.py` 新增留言 CRUD + 附件 CRUD（upload / get / delete / download）
- [x] 附件儲存於 `uploads/task_files/`，`original_filename` 備份（解決 `secure_filename` 過濾中文）
- [x] 前端 `taskService.js` 補完留言 + 附件方法
- [x] `TimelineDetailDialog.vue` 任務詳情 — 附件預覽 / 下載 / 刪除 + 留言
- [x] `TasksView.vue` 任務點擊 → 詳情 Modal（附件 + 留言）
- [x] 下載改用 fetch+blob（解決跨來源 `<a download>` 無效）
- [x] 重複路由整合：`timelines.py` 移除 6 條與 `tasks.py` 重複的路由
- [x] 所有 model 補上 `cascade='all, delete-orphan'`（TaskComment、TaskUser、Subtask、TaskFile、TimelineUser）

---

### 3. 垃圾桶機制 ✅ 完成（2026/03/02）

- [x] 後端新建 `trash.py` blueprint：查詢 / 還原 / 永久刪除 API（任務 + 專案各一套）
- [x] 永久刪除專案時同步清除所有子任務、附件（cascade + `os.remove`）
- [x] 前端 `trashService.js`
- [x] `TrashView.vue`：已刪專案 + 已刪任務，還原 / 永久刪除，非建立者唯讀
- [x] Router + Sidebar 入口

---

### 技術債（Phase 1 結束，已全部完成 ✅）

- [x] `stores/timelines.js` — 03/02 完成建立，TimelinesView 改用
- [x] `stores/groups.js`、`stores/profile.js` — 03/02 完成，View 同步更新
- [x] Subtask 前端 UI — `TasksView.vue` + `TimelineDetailDialog.vue` 任務詳情 Modal（03/02）
- [ ] `utils/formatters.js` — `formatDate()` 在多個元件重複定義，待 Phase 2 整理

---

### 4. Token 刷新機制 ✅ 已完成

- 後端 `/auth/refresh` endpoint ✅
- 前端 Response 攔截器自動刷新 ✅
- 防止競態條件（Queue 機制）✅

---

## Phase 2 - 功能完善

> **狀態：Phase 1、Phase 2 全部完成 ✅，目前進入 Phase 3（TypeScript 遷移）**

### 5. 通知系統 ✅ 完成（2026/03/06）

**後端：**
- [x] `timelines.py`：`add_timeline_member` 邀請成員後自動寫入 `Notification`（type=`timeline_invited`）
- [x] `timelines.py`：新增 `GET /api/timelines/upcoming` — 3 天內截止 OR 進度 ≥80%
- [x] `tasks.py`：新增 `GET /api/tasks/upcoming` — 3 天內截止 OR 進度 ≥80%（自建 + 指派 + timeline 成員）

**前端：**
- [x] `Header.vue`：30 秒輪詢 `fetchUnreadCount()`（`setInterval` + `onUnmounted` 清除避免洩漏）
- [x] `Header.vue`：`notifIcon` map 補上 `timeline_invited: '👥'`
- [x] `taskService.js` / `timelineService.js`：新增 `upcoming()` 方法
- [x] `HomeView.vue`：完整重寫，新增「⏰ 即將到期 / 進度落後」區塊，逾期紅色 / 即將到期琥珀色標記

### Timeline 任務成員指派 ✅ 完成（2026/03/06）

- [x] `TimelineDetailDialog.vue`：任務列表每行加 👥 指派按鈕（負責人 role=0 可見）
- [x] `TimelineDetailDialog.vue`：任務詳情 Dialog 內嵌指派成員區塊（已指派標籤 + 快速指派按鈕）
- [x] `openTaskDetail()` 自動載入 timeline 成員列表

### 6. 數據分析儀表板 ✅ 完成（2026/03/07）

**實作方式：整合至 ProfileView.vue（不另開新路由）**

**後端：**
- [x] `profile.py`：新增 `GET /api/profile/chart-stats` — 近 30 天完成趨勢＋狀態分布＋各專案任務量前 8
- [x] `timelines.py`：新增 `GET /api/timelines/:id/member-stats` — 刈員貢獻 + 專案狀態分布（`@require_timeline_role('owner')` 保護）
- [x] chart-stats 查詢涉及「自建 + 直接指派 + timeline 成員」的專案 ID 不重合算

**前端：**
- [x] `profileService.js`：新增 `getChartStats()`
- [x] `timelineService.js`：新增 `getMemberStats(id)`
- [x] `stores/profile.js`：新增 `ownedTimelines`、`chartStats`、`chartLoading`、`fetchChartStats()`
- [x] `ProfileView.vue` 整合 ECharts，在統計卡下方新增兩層圖表區塊：

| 層級 | 內容 | 權限 |
|------|------|------|
| Level 1 個人 | 近 30 天完成趨勢折線圖 + 狀態甜甜圈圖 + 各專案任務量橫條圖 | 所有登入用戶 |
| Level 2 專案 | 下拉選擇專案→成員貢獻橫條圖 + 專案狀態甜甜圈圖 | 僅 role=0 負責人可見 |

### 7. 行動版優化 ✅ 完成（2026/03/11）
- [x] 手機：底部導航列 `md:hidden fixed bottom-0 z-9999`，五個頁籤
- [x] 手機：`App.vue` `isMobile` 偵測，`sidebarOpen` 預設 false，單欄 Grid
- [x] 桌面：左側 Sidebar 維持（`hidden md:block fixed`）
- [x] `Header.vue` 手機版收縮（padding 縮短 / 用戶名隱藏 / 通知面板寬度限制）
- [x] 全部 View 加 `pb-24 md:pb-6`，防止內容被底部導航遮蓋
- [x] `TimelineViewModes.vue` 最外層加 `mb-20 md:mb-0`，確保最後一張卡片可捲出導航以上
- [x] 修復 `style.css` `* { padding: 0 }` 未包進 `@layer base` 導致全站 Tailwind padding 失效

### 8. 全站 alert/confirm 換 Toast ✅ 完成（2026/03/07）
- [x] `composables/useConfirm.ts` — Promise-based 單例狀態管理，暴露 `confirm()` + `dialogState`
- [x] `components/ConfirmDialog.vue` — Headless UI Dialog，在 `App.vue` 全局挂載
- [x] 全站 6 個檔案 16 處 `confirm()` 全部替換為 `await confirm({ title, message })`（TasksView / TimelinesView / TrashView / GroupsView / TodosView / TimelineDetailDialog）
- [x] 所有 `alert()` 已在先前隨 vue-sonner Toast 導入時完成

---

## Phase 3 - TypeScript 遷移

> **前提：JavaScript 重構完成且穩定運行後**
> **原則：漸進式遷移，不需一次全改**
> **最後更新：2026/03/13（基礎層 + Service + Store + Composables ✅ 完成）**

### 遷移順序（由底層到上層）

1. **基礎層** ✅ 完成（2026/03/13）
   - [x] TypeScript 5.9.3 安裝為 devDependency
   - [x] `tsconfig.json` — `strict: true`、`allowJs: true`（漸進式遷移）、`moduleResolution: bundler`、`types: ["vite/client"]`
   - [x] `vite.config.js` — 新增 `@` alias（fileURLToPath + node:url）
   - [x] `utils/formatters.ts` — 所有參數標注 `string | null | undefined`，回傳 `string` / `boolean`
   - [x] `utils/validators.ts` — 新建驗證工具（validateRequired / validateEmail / validatePassword / validateConfirmPassword / validateDateRange / validateMaxLength）
   - [x] `types/index.ts` — 定義共用型別（Timeline、Task、Todo、User、Group、Message、Notification、Profile、ChartStats、DaysRemainingResult 等）

2. **Service 層** ✅ 完成（2026/03/13）
   - [x] `services/api.ts` — `RetryableAxiosRequestConfig` 擴充 `_retry`，`FailedQueueItem` 型別，`processQueue` 加型別
  - [x] `services/socketService.ts`
   - [x] `services/todoService.ts`
   - [x] `services/taskService.ts`
   - [x] `services/timelineService.ts`
   - [x] `services/groupService.ts`
   - [x] `services/profileService.ts`
   - [x] `services/trashService.ts`
  - [x] `services/notificationService.ts`（補漏遷移）
   - [x] 舊 `.js` 檔已全部刪除

3. **Store 層** ✅ 完成（2026/03/13）
  - [x] `stores/auth.js` → `auth.ts`
  - [x] `stores/todos.js` → `todos.ts`
  - [x] `stores/tasks.js` → `tasks.ts`
  - [x] `stores/timelines.js` → `timelines.ts`
  - [x] `stores/groups.js` → `groups.ts`
  - [x] `stores/profile.js` → `profile.ts`
  - [x] `stores/notifications.js` → `notifications.ts`
   > ⚠️ `stores/ui.js` 從未建立（toast 由 vue-sonner 直接呼叫，sidebar 狀態留在 App.vue），不需遷移

4. **Composables** ✅ 完成（2026/03/13）
  - [x] `composables/useConfirm.js` → `useConfirm.ts`（目前唯一存在的 composable）
   > 💡 Phase 3 可考慮將 `getDaysRemaining` / `getTimelineStatus`（目前分別在 `stores/timelines.js`
   > 和 `TimelineViewModes.vue` 各有一份本地副本）提取到 `composables/useTimelineStatus.ts`

5. **元件層** ✅ 完成（2026/03/15）
  - [x] `<script setup>` → `<script setup lang="ts">`
  - [x] 定義 Props/Emits 型別
  - [x] 逐一遷移 components 和 views

### TS 型別定義範例

> ⚠️ 注意：後端 API 回傳 snake_case，store 轉換後前端使用 camelCase。
> `types/index.ts` 應定義**前端使用的 camelCase 版本**。

```typescript
// types/index.ts
export interface User {
  id: number;
  name: string;
  username: string | null;
  email: string;
  phone: string | null;
}

export interface Todo {
  id: number;
  title: string;
  content: string | null;
  completed: boolean;
  userId: number;
  createdAt: string;
}

// 前端 store 內的 Timeline 物件（已由 API snake_case 轉換）
export interface Timeline {
  id: number;
  name: string;
  description: string | null;
  startDate: string | null;  // 來自 API start_date
  endDate: string | null;    // 來自 API end_date
  role: 0 | 1;               // 0=負責人, 1=成員
  totalTasks: number;
  completedTasks: number;
  createdBy: number;
}

// getDaysRemaining 回傳型別（timelines store + TimelineViewModes 共用）
export interface DaysRemainingResult {
  days: number | null;
  text: string;
  display: string;
  colorClass: string;
}

export interface Task {
  taskId: number;
  name: string;
  taskRemark: string | null;
  completed: boolean;
  startDate: string | null;
  endDate: string | null;
  timelineId: number | null;
  isOwner: boolean;
}
```

### Vite 設定調整

```javascript
// vite.config.js - 無需大改，Vite 原生支援 TS
// 只要安裝 typescript 和調整 tsconfig.json 即可
```

---

## Phase 4 - 進階功能

> **分子階段進行：Payload Contract / Guardrails 已完成，其餘持續規劃**

### 7. Payload Contract / Guardrails ✅ 完成（2026/03/15）
- 前端新增 `utils/payloadMappers.ts`，統一 `Create/Update` payload 輸出
- 前端 service 回傳型別與後端 mutation response 對齊
- 後端 mutation endpoint 採 allowlist + unknown field reject
- 新增 `npm run guardrails:payload` 靜態檢查（禁止 `Partial<Entity>` 與 `service.update(...{...entity})`）
- 新增 `docs/reference/payload-contracts.md` 作為端點契約文件

### 8. 甘特圖視覺化
- 整合 `frappe-gantt`（Side Project 輕量方案）
- 任務依賴關係（基礎版：同專案任務依開始日自動串接）
- 拖曳調整時間（可回寫任務 start_date / end_date）

### 9. 後端 Service Layer 重構 ✅ 完成（2026/03/16）
- 抽離 Blueprint 中的商業邏輯
- 新增 `backend/services/` 資料夾
- 減少 Blueprint 直接操作資料庫

**完成明細（2026/03/16）：**
- [x] 建立 `backend/services/` 與 `__init__.py`
- [x] 新增 `services/task_service.py`（任務權限 decorator、角色判斷、allowed fields、通知 helper）
- [x] 新增 `services/timeline_service.py`（專案權限 decorator、角色判斷、allowed fields）
- [x] 新增 `services/todo_service.py`（Todo allowlist + serializer）
- [x] 新增 `services/profile_service.py`（Profile allowlist + serializer）
- [x] 新增 `services/group_service.py`（群組 invite code + serializers）
- [x] 新增 `services/notification_service.py`（通知 serializer）
- [x] 新增 `services/trash_service.py`（垃圾桶 serializer + 檔案清除 helper）
- [x] 新增 `services/auth_service.py`（auth/current user serializer）
- [x] 新增 `services/message_service.py`（未讀訊息 query helper）
- [x] `blueprints/tasks.py` 改為引用 task service（移除重複 helper）
- [x] `blueprints/timelines.py` 改為引用 timeline service（移除重複 helper）
- [x] `blueprints/todos.py` / `profile.py` / `groups.py` / `notifications.py` / `trash.py` / `auth.py` / `messages.py` 改為引用對應 service
- [x] `python -m compileall blueprints services models app.py` 驗證通過

### 10. 資料庫遷移 ✅ 完成（2026/03/22）
- SQLite → PostgreSQL 遷移已完成（Supabase PostgreSQL）。
- 詳細流程與驗證紀錄請見下方 Phase 5.1。

### 11. 即時通訊
- WebSocket (Flask-SocketIO)
- 群組即時聊天
- [x] 2026/03/18 已完成實施規劃，文件：`docs/WebSocket_群組即時聊天計畫.md`
- [x] Slice A：後端 Socket 接線（threading + JWT 握手 + group room 廣播）
- [x] Slice B：前端 Socket 疊加（Groups store/view）並保留 REST fallback
- [x] Slice C：授權/隔離/重連與回退驗證
- [x] 後端事件層重整：`backend/socket_events.py` → `backend/realtime/socket_events.py`
- [x] 啟動流程修正：`start_all.bat` 後端改由 `python app.py` 啟動 Socket server
- [x] 驗證完成：`python -m compileall blueprints services models realtime app.py` 通過

---

## Phase 5 - 上線與部署整合（已完成）

> **狀態：5.1~5.6 已完成（不含 AI 任務生成）；後端 CI coverage 報告工作流已上線。**

### 核心成果（摘要）
- [x] 5.1 SQLite -> PostgreSQL（Supabase）完成，資料庫主線可運行
- [x] 5.2 Railway 後端部署完成（Gunicorn + Eventlet + 健康檢查）
- [x] 5.3 Firebase 前端部署完成，前後端與資料庫連通
- [x] 5.4A 單人核心流程驗收完成（本輪排除 AI 任務生成）
- [x] 5.5 前端測試基線完成（85/85）
- [x] 5.6 後端測試與 coverage 報告落地

### AI 主線策略（Phase 5 決議）
- Phase 5 產品主線只做 A/B：
  - A：`n8n + MCP` 工作流整合
  - B：`Prompt Engineering + CoT`
- C 之後主題（微調/安全/多模態/GUI Agent）改走 `labs/` 學習支線，不直接塞進主專案
- Lab 成果若要回流主線，需通過品質/成本/安全/可維運四項門檻

### Phase 5 執行明細（Railway + Supabase + Firebase）

> **目標：將 PrAjeKt 部署到生產環境，展示用可靠基礎**  
> **方案：方案 B - 略付費但順暢版（月費 $5-10）**  
> **架構：前端 Firebase | 後端 Railway WebSocket | 資料庫 Supabase PostgreSQL**  
> **完成日期：2026/03/22（Phase 5.1/5.2/5.3 完成）**

### 5.1 | 資料庫遷移（SQLite → PostgreSQL）✅ 完成

- [x] Supabase 免費專案建立 + PostgreSQL 連線字串取得
- [x] 後端改用 `psycopg2` driver，直接使用 `init_db.py` 建立 15 個表格
- [x] 本地端對端驗證（新帳號註冊 → 完整流程驗證通過）
- [x] 環境變數設定（DATABASE_URL、SECRET_KEY、JWT_SECRET_KEY）
- [x] 流程文檔編寫：[Phase5_1_PostgreSQL遷移流程.md](runbooks/Phase5_1_PostgreSQL遷移流程.md)

### 5.2 | 後端部署到 Railway ✅ 完成

- [x] GitHub 授權 + Railway 專案建立 + main 分支自動部署
- [x] 環境變數設定（DATABASE_URL、SECRET_KEY、JWT_SECRET_KEY、FLASK_ENV）
- [x] Flask 啟動改為支援 PORT 環變（Railway 動態分配）
- [x] Procfile + start.sh + railway.json 配置
- [x] Gunicorn + Eventlet 生產伺服器配置
- [x] 首次自動部署成功，公網 URL：`https://prajekt-production.up.railway.app`
- [x] 資料庫初始化成功，15 個表格建立完成
- [x] 健康檢查驗證（`GET /api/health → 200 OK`）
- [x] 循環導入問題修復（4 波 commit）
- [x] 日誌監控與 WebSocket 連線驗證
- [x] 流程文檔編寫：[Phase5_2_Railway後端部署.md](runbooks/Phase5_2_Railway後端部署.md)

### 5.3 | 前端調整與 Firebase 部署 ✅ 完成

- [x] 取得 Railway 後端實際 URL（`prajekt-production.up.railway.app`）
- [x] `frontend/.env.production` 指向 Railway 後端 URL
- [x] `npm run build` 驗證成功（除 chunk 大小警告外無編譯錯誤）
- [x] Firebase 前端編譯優化（預檢查環境、依賴、配置）
- [x] Firebase Hosting 部署成功：`https://prajekt-kiriris.web.app`
- [x] firebase.json 配置（公開目錄、SPA rewrite）
- [x] CORS 配置更新（backend/app.py 允許 `https://prajekt-kiriris.web.app`）
- [x] Commit 0b0d6e0 推送至 GitHub，Railway 自動重新部署搭載新 CORS 設定
- [x] 三服務完整聯動驗證通過
- [x] 流程文檔編寫：[Phase5_3_Firebase前端部署.md](runbooks/Phase5_3_Firebase前端部署.md)

### 5.4 | 上線範圍收斂 ✅ 收斂完成（本輪）

#### 5.4A | 上線範圍收斂（本輪）
- [x] 單人核心流程完成確認（登入 → 建立任務 → 群組聊天 → 任務操作 → 待辦管理）
- [x] 生產環境健康檢查完成（`GET /api/health` 回應 200；`/socket.io` 握手 200）
- [x] GitHub README 更新（生產環境 URL、快速開始指南）
- [x] 驗收範圍註記：本輪上線不包含 AI 任務生成

**預期成果**：
- ✅ PrAjeKt 線上運行（Firebase 前端 + Railway 後端 + Supabase 資料庫）
- ✅ 月費成本：$5-10（僅 Railway 後端），無前端與資料庫額外費用
- ✅ 本輪驗收完成（單人核心流程），可用於展示
- ✅ 本輪上線範圍已明確排除 AI 任務生成

### 5.5 | 前端測試 ✅ 完成（第一輪基線，2026/03/25）

> **目標：建立全面的前端單元測試套件，保證核心功能可靠性**  
> **工具：Vitest 4.1.1 + Vue Test Utils + Pinia 測試模式**  
> **狀態：85 個測試全數通過，平均覆蓋率 88.85%**

#### 已完成測試範圍

**Services 層（8 個檔案，24 個測試）✅**
- `groupService.ts`（6 tests，100% coverage）— CRUD、成員管理端點映射驗證
- `timelineService.ts`（3 tests，100%）— 專案相關 API 端點驗證
- `taskService.ts`（2 tests，100%）— 任務、子任務、成員、留言、檔案端點驗證
- `profileService.ts`, `notificationService.ts`, `todoService.ts`, `trashService.ts`（各 1 test，100%）— 端點映射驗證
- `socketService.ts`（5 tests，100%）— Socket 連線與環境變數解析
- `socketService.env.ts`（4 tests，90% branch）— SQLite 上傳路境與 VITE_SOCKET_URL 優先度
- `api.ts`（5 tests，80.76% statements）— Token 刷新、401 處理、競態防護、隊列機制

**Stores 層（7 個檔案，35 個測試，平均 83.24%）✅**
- `auth.ts`（3 tests，80%）— 登入、登出、Token 持久化
- `todos.ts`（6 tests，92.85%）— CRUD、完成狀態切換、篩選清單
- `tasks.ts`（5 tests，93.33%）— CRUD、子任務操作
- `timelines.ts`（7 tests，79.74%）— 日期計算、CRUD、任務操作、成員管理
- `groups.ts`（6 tests，76.98%）— Socket 事件處理、訊息操作、清理生命周期
- `notifications.ts`（3 tests，85.71%）— 取得、標記已讀、刪除
- `profile.ts`（5 tests，97.36%）— 個人資料取得、統計計算、圖表資料

**Utils 層（3 個檔案，17 個測試，97.02%）✅**
- `formatters.ts`（5 tests，100%）— 日期格式化、檔案類型偵測、檔案大小格式化
- `validators.ts`（6 tests，100%）— 必填、Email、密碼強度、日期範圍、長度驗證
- `payloadMappers.ts`（6 tests，94.84%）— 建立/更新 Task/Todo 的 payload 正規化

**Composables 層（1 個檔案，4 個測試）✅**
- `useConfirm.ts`（4 tests，100%）— 確認對話框狀態管理與事件流

#### 測試覆蓋品質評估

| 指標 | 達成 |
|------|------|
| **Tests 通過率** | ✅ 85/85（100%）|
| **TypeScript 型別檢查** | ✅ 0 errors |
| **Average Statements Coverage** | ✅ 88.85% |
| **Branch Coverage** | ⚠️ 71.56%（嵌套/邊界測試缺口）|
| **Functions Coverage** | ✅ 88.61% |
| **Untested Modules** | ⚠️ Views (8)、Components (UI 層)、Router guards |

#### 後續測試規劃（可選）

**即期補強（高投資報酬）**
- [ ] `api.ts` 額外 401 邊界情況（timeout、network error）
- [ ] `stores/auth.ts` register/fetchCurrentUser error path
- [ ] `stores/timelines.ts` 多層 error scenarios

**中期計畫（非必須）**
- [ ] Component Unit Tests（Header、Sidebar、TaskCard、TimelineCard）
- [ ] Router Guard & redirect 邏輯
- [ ] E2E 集成測試（Playwright/Cypress）

### 5.6 | 後端單元測試 ✅ 完成（2026/03/29）

> **目標：建立後端 API 路由與 business logic 的單元測試**  
> **工具：pytest + pytest-flask + SQLAlchemy test fixtures**  
> **狀態：主要 blueprint（auth/todos/tasks/timelines/groups/profile/notifications/trash/messages）與 core services 測試完成，CI 已啟用 coverage 報告（term/xml/html）**

#### 建議測試覆蓋範圍

**Blueprint 層（路由驗證與權限檢查）**
- [x] `blueprints/auth.py` — login/register/refresh/logout 的正常流程與錯誤情況
- [x] `blueprints/tasks.py` — 第一波：auth guard、建立/查詢/刪除軟刪除主流程
- [x] `blueprints/timelines.py` — 第一波：建立/查詢/更新驗證/刪除連動軟刪除
- [x] `blueprints/todos.py` — CRUD、toggle 邏輯
- [x] `blueprints/groups.py` — 第一波：建立、加入、離開、訊息存取權限
- [x] `blueprints/profile.py` — get/update + search + chart-stats
- [x] `blueprints/notifications.py` — fetch/unread-count/mark-read/read-all/delete
- [x] `blueprints/trash.py` — restore/permanently delete + cascade 清理

**Service 層（business logic 與 serializers）**
- [x] `services/task_service.py` — allowlist validation、role-based access
- [x] `services/timeline_service.py` — 角色繼承邏輯、成員查詢
- [x] `services/auth_service.py` — serializer 與 user payload 驗證
- [x] `services/notification_service.py` — 通知資料序列化驗證
- [x] `services/group_service.py` / `message_service.py` / `profile_service.py` / `todo_service.py` — serializer/helper/query 邏輯驗證

**Model 層（ORM 關聯與定義）**
- [x] `models/user.py` — 必填/唯一性/`__repr__`
- [x] `models/task.py` — defaults/cascade delete
- [x] `models/timeline.py` — member cascade/關聯完整性

#### 測試基礎設施建議

```python
# tests/conftest.py
@pytest.fixture
def app():
    """Flask app 測試實例，使用 in-memory SQLite"""
    app = create_app('testing')
    with app.app_context():
        db.create_all()
        yield app
        db.session.remove()
        db.drop_all()

@pytest.fixture
def client(app):
    """測試 HTTP client"""
    return app.test_client()

@pytest.fixture
def test_user(app):
    """準備測試用戶"""
    user = User(...)
    db.session.add(user)
    db.session.commit()
    return user
```

#### 後續行動
- [x] 建立 `tests/` 目錄與 `conftest.py`
- [x] 第一波實施完成（models + auth/todos blueprint + core services）
- [x] 第二波已完成主要藍圖（tasks/timelines/groups）
- [x] 第三波擴充完成（profile/notifications/trash blueprints）
- [x] 整合到 CI/CD（GitHub Actions：PR 時自動跑 pytest）
- [x] 產出 coverage 報告（term/xml/html）

#### 驗證結果（本輪）
- 後端測試結果：`108 passed`
- 覆蓋率結果：`80.59%`（作為目前基線）
- 路由層與 service 層的權限、cascade、序列化邏輯均有對應測試

---

## CI/CD（Side Project 輕量版）

> **目標：在不增加維護負擔的前提下，建立最小可用的自動化品質門檻。**

### C1 | Pull Request 基本檢查（GitHub Actions）
- [ ] Frontend：`npm ci` + `npm run test:run` + `npm run build`
- [ ] Payload 契約守門：`npm run guardrails:payload`
- [x] Backend：GitHub Actions 已啟用 `pytest + coverage`（term/xml/html）

### C2 | Branch 保護策略（精簡）
- [ ] `main` 啟用 required status checks（至少 Frontend Build + Frontend Tests）
- [ ] 禁止直接 push `main`（改走 PR merge）

### C3 | 部署流程（維持現況，自動化最小化）
- [x] 後端：Railway 由 `main` 自動部署
- [x] 前端：Firebase Hosting 既有部署流程可用
- [ ] 補一份 `docs/guides/CI_CD_最小流程.md`（提交規範 + 發版步驟 + 回滾步驟）

---

## Phase 6 - AI 產品化與資料層統一（2026/04 規劃開始）

> **前提：Phase 5 已完成上線；Phase 6 以「可維運」為優先，不新增複雜中介平台。**
>
> **技術策略：Gemini（主）+ 可替換 AI Provider 抽象層 + PostgreSQL 統一本地/部署資料層。**
>
> **主線策略：先把開發資料庫遷移到 PostgreSQL，再推進 RAG-B / RAG-C / MCP。**

### Phase 6 分階段清單

| 階段 | 內容 | 週期 | 狀態 |
|-----|------|------|------|
| 6.0 | 開發資料庫遷移（SQLite → PostgreSQL） | Week 1-2 | ✅ 核心完成 |
| 6.1 | AI Provider 收斂（Gemini + Adapter） | Week 2-3 | ✅ 核心完成 |
| 6.2 | RAG-A：Task Comment 智能摘要（決議/風險/下一步） | Week 3-4 | ✅ 核心完成 |
| 6.3 | RAG-B：Group 知識快照（近 30 天訊息+評論） | Week 5-6 | 🟡 核心完成（Digest v1） |
| 6.4 | RAG-C：個人週回顧（本週完成/阻礙/下週重點） | Week 7-8 | ⏳ 待開始 |
| 6.5 | MCP 接線與文檔收斂（Workflow/Prompt/Runbook） | Week 9-10 | 🟡 進行中（Copilot + MCP 整合已完成） |
| 6.6 | LangChain 遷移、統一 AI 流程、前後端文檔化 | Week X | ✅ 基礎完成（2026/04/11） |

### 6.0 | 開發資料庫遷移（SQLite → PostgreSQL）

**詳細流程文檔**：[Phase6_0_開發資料庫遷移流程.md](runbooks/Phase6_0_開發資料庫遷移流程.md)

**目標**：讓本地開發、測試與部署資料庫一致，減少「本地可跑、部署異常」的環境偏差。

**待辦清單：**
- [x] 建立本地 PostgreSQL 容器（docker compose）與持久化 volume
- [x] 統一本地 `DATABASE_URL`（`.env.local` 切換至 PostgreSQL 主線）
- [x] 補一份 SQLite → PostgreSQL 遷移腳本（`backend/migrate_sqlite_to_postgres.py`）
- [x] 更新 migration 指引並完成全新資料庫 `flask db upgrade` 驗證
- [x] 更新 `start_all.bat`（PostgreSQL 主線）與 `README` 快速部署指引
- [x] 新增 `bootstrap_pg_local.bat` 一鍵初始化腳本（啟容器 → migrate schema → 資料遷移 → 啟動）

**本輪執行結果（2026/04/04）**：
- SQLite → PostgreSQL 已完成遷移，主要資料表筆數對帳一致（users/groups/timelines/tasks/todos/messages/task_comments/subtasks 等）。
- 遷移後驗證：`/api/health` 正常回傳 PostgreSQL 連線、後端回歸測試 `136 passed`。
- migration 鏈補強完成：修復舊 migration 在 fresh PostgreSQL 的兼容問題，`flask db upgrade` 可從空庫跑到 head。

**驗收條件：**
- [x] `backend` 本地啟動改用 PostgreSQL
- [x] `pytest tests -q` 在 PostgreSQL 環境通過
- [x] `/api/health` 回傳資料庫資訊為 PostgreSQL

---

### 6.1 | AI Provider 收斂（Gemini + Adapter）

**目標**：保留 AI 功能彈性，但避免引入額外平台複雜度。

**待辦清單：**
- [x] 任務留言摘要 API 改為直接使用 Gemini（已上線）
- [x] 移除棄用本地模型呼叫 dead code（`task_service.py`）
- [x] 抽象 `ai provider` 介面（例如 `gemini` / `mock` / `future-provider`）
- [x] 以環境變數切換 provider，避免把模型綁死在 service 內

**備註：** 已完成抽象層與環境變數切換（backend/services/ai_provider.py），可透過 `AI_PROVIDER` 與 `AI_MODEL` 切換提供者與模型（完成日：2026-04-04）。

---

### 6.2 | RAG-A：Task Comment 智能摘要（Week 3-4）✅ 核心實裝完成

**目前成果：**
- [x] `POST /api/tasks/{task_id}/ai-comment-summary` 完成（含 member 權限驗證）
- [x] `build_task_comment_summary_context()` 與 payload normalization 已收斂
- [x] 前端任務詳情「AI 摘要」按鈕與結果卡片接線完成
- [x] 實測回應穩定（Gemini 路線）

---

### 6.3 | RAG-B：Group 知識快照（Week 5-6）

**待辦清單：**
- [x] 新增 `POST /api/groups/{group_id}/ai-snapshot`
- [x] 建立「最近 30 天」資料抓取器
- [x] 前端 GroupsView Snapshot 卡片
- [x] 摘要輸出收斂為「行動導向 Digest」：一句重點 / 你要做什麼 / 風險阻塞 / 精簡決議
- [x] 後端測試更新（Blueprint + Service）與回歸通過（後端 `144 passed`）
- [ ] RAG-B 評測記錄（摘要準確度、可追溯性）
- [ ] 群組-專案關聯（Group ↔ Timeline）
- [ ] 群聊後一鍵建立任務（AI 建議任務 -> 專案任務建立）

**6.3 下一步（實用化收斂）**
- [ ] 新增 group 與 timeline 的關聯表與管理 API
- [ ] 在快照結果中加入「建議任務草稿」欄位（name / remark / owner_hint）
- [ ] 新增 `從快照建立任務` 流程（前端確認後寫入 `tasks`）

---

### 6.4 | RAG-C：個人週回顧（Week 7-8）

**待辦清單：**
- [ ] 新增 `ai_weekly_reviews` 資料表（輕量欄位）
- [ ] 新增建立/查詢週回顧 API
- [ ] ProfileView 回顧面板與歷史列表
- [ ] RAG-C 評測記錄（實用性 1~5 分）

---

### 6.5 | MCP 接線與文檔收斂（Week 9-10）🟡 進行中（2/3 工具）

**待辦清單：**
- [x] 新建 `mcp_server.py`，封裝核心工具（先接 6.2 / 6.3）
  - [x] `task_comment_summary(task_id)`
  - [x] `group_snapshot(group_id)`
  - [ ] `weekly_review(user_id, week)`（延後：待 6.4 API 與資料表完成後再接）
- [x] 文檔輸出
  - [x] `docs/phase6-local-setup.md`
  - [x] `docs/phase6-rag-evals.md`
  - [x] `docs/phase6-mcp-tools.md`（已補充 `timeline_generate_tasks` / `timeline_batch_create_tasks`）
  - [x] `docs/workflows/`

**驗收條件：**
- [x] 已上線工具可透過 MCP 介面呼叫（`task_comment_summary`、`group_snapshot`）
- [ ] 三工具完整驗收（待 `weekly_review` 實作後補）
- [x] 文檔可在新機器重現本地環境

---

### 6.6 | LangChain 遷移、AI 流程統一、文檔化 ✅ 完成（2026/04/11）

**目標：**
- [x] 統一後端 AI 調用路徑（Service -> Chain -> LLM）
- [x] 讓 PromptTemplate、OutputParser 與模型配置集中管理
- [x] 為 Phase 7 多步工作流保留可擴展路徑

**完成項目：**
- [x] 依賴收斂：`langchain==0.1.15`、`langchain-google-genai==0.0.15`、`langchain-core==0.1.35`
- [x] 建立 `backend/prompts/`（`tool_selector.py`、`task_generator.py`、`summary_templates.py`）
- [x] 建立 `backend/chains/`（`prompt_manager.py`、`task_generation_chain.py`、`tool_selection_chain.py`、`summary_chain.py`、`llm_factory.py`、`workflows.py`）
- [x] PromptTemplate 跳脫修復：JSON 範例 `{ }` -> `{{ }}`
- [x] 清理舊 fallback 分支與舊解析函式，服務層路徑統一到 LangChain
- [x] 測試驗證：Phase 6.6 相關與全量回歸驗證通過（153 passed）
- [x] 文檔完成：`docs/Phase6_6_LangChain_Migration_Guide.md`（架構圖、14 步流程、責任分工）

**驗收條件（已滿足）：**
- [x] 既有 API 契約不變（前端無感改動）
- [x] 工具路由與摘要流程穩定（AI 異常時保留 keyword fallback）
- [x] 開發者可依文檔快速追蹤前端 -> 後端 -> Chain -> LLM 全流程

**後續（若啟動 6.6 第二階段）：**
- [ ] 進一步將 `copilot_service.py` prompt 邏輯完全收斂到 PromptTemplate + Chain
- [ ] 補齊向後相容與回歸性能檢查（格式一致 + 回應時間基線）

---

## Phase 7 - AI 實用化精簡路線（2026/04）

> **主旨：先做「有效且可落地」功能，避免功能膨脹。**
> 詳細執行版：`Phase7_精簡執行版.md`

### 核心目標（只留兩項）

1. 降低延期風險（提前發現衝突與風險）。
2. 降低週報成本（可直接使用的一鍵週報）。

### 7.1 | 週報 + 衝突檢查 MVP ✅ 完成（2026/04/17）

- [x] Task 模型新增 `completed_at`（可為 null）
- [x] 週報生成能力已落地（整合於 `timeline_service.py` 的 `build_weekly_report_for_timeline`）
- [x] 衝突檢查能力已落地（整合於 `timeline_service.py` 的 `check_timeline_task_conflicts`）
- [x] 新增 API：
  - [x] `GET /api/timelines/{timeline_id}/weekly-report`
  - [x] `POST /api/timelines/{timeline_id}/conflict-check`
- [x] 前端補 `WeeklyReport` / `ResourceConflict` 型別與 `timelineService` 呼叫
- [x] 任務建立/編輯流程加入衝突預檢
- [x] 新增任務表單支援 `assignee_user_ids` 多選指派
- [x] 衝突預覽加入隱私遮罩（非本人指派時遮罩任務名，過載任務名改顯示件數）

### 7.2 | 進度風險分析 MVP 🟡 核心完成（2026/04/20）

- [x] Task 模型新增 `depends_on_task_ids`（JSON array）與 migration
- [x] 新增 `critical_path_service.py`（DAG 分析、循環依賴降級、風險分級）
- [x] 新增 API：
  - [x] `GET /api/timelines/{timeline_id}/risk-analysis`
  - [x] `POST /api/timelines/{timeline_id}/risk-analysis/notify`（owner）
- [x] 前端補 `CriticalPathAnalysis` / `RiskItem` 型別與風險分析 UI
- [x] 任務建立/編輯支援設定依賴任務（`depends_on_task_ids`）
- [x] 風險分析面板新增「產生依賴圖」按鈕（SVG 自動佈局，可重新產生）
- [x] AI 任務生成/批次建立支援依賴欄位（`depends_on_task_ids` + `depends_on_task_refs`）
- [x] AI 未提供依賴時套用順序鏈 fallback，避免全空依賴
- [ ] 後續補強：風險通知 UI 入口與 7.2 互動測試案例

### 7.3 | 知識增強規劃（內部經驗版）✅ 核心閉環完成（2026/05/08）

- [x] 新增 `knowledge_documents` / `knowledge_chunks` 與 pgvector migration（含 per-user sha256 unique）
- [x] 新增 `knowledge_service.py`（上傳/解析/索引/重建）與 `text_splitter_service.py`（結構化切塊）
- [x] 新增 `embedding_service.py`，改為 LangChain Embeddings provider 架構（預設 `gemini-embedding-2`）
- [x] 新增 `rag_planning_service.py`（歷史任務 + 個人知識檢索 + 來源加權）
- [x] 新增 API：`POST /api/timelines/ai-suggest-plan`
- [x] 新增知識文件 API：`/api/knowledge/documents`（上傳/列表/刪除/reindex）
- [x] 規劃結果附來源依據（`source_references`），且無來源時拒絕輸出（422）
- [x] 前端補 `AIPlanSuggestion` / `SourceReference` 型別與規劃助手 UI 串接
- [x] Source type 契約收斂為 `timeline_task` / `knowledge_chunk`，不新增 `project_knowledge_document`
- [x] 個人知識庫頁 `/knowledge` 完成：上傳、列表、狀態、搜尋、篩選、排序、刪除、reindex
- [x] Project Files 落地於 `TimelineDetailDialog.vue`：project-scoped upload/list/filter/batch delete/batch reindex/download/preview/events
- [x] 後端補 project-scoped knowledge storage/events，並修正 migration graph revision 鏈
- [x] RAG 規劃支援專案知識檢索、vector miss 文字 fallback、LLM timeout fallback
- [ ] 後續重構：拆分 `TimelineDetailDialog.vue` 內的 Project Files 與 RAG Planning 面板
- [ ] 後續品質：建立固定評測案例，調整來源排序、prompt 與輸出驗收條件

### 資料模型策略（先可用，後優化）

- [x] `completed_at` 已補齊，週報可用性已提升
- [x] `depends_on_task_ids` 第一版已採用 JSON
- [ ] 任務依賴「正規化」改列後續優化項，Phase 7 不阻塞主線

### 第一版不做（避免無用功能）

- [ ] PDF 匯出
- [ ] 定時自動掃描
- [ ] 外部搜尋 provider 整合
- [ ] MCP 工具擴充

### 驗收門檻

- [x] 7.1：可用週報 + 可用衝突提示
- [x] 7.2：可計算 critical path + 可讀風險建議（MVP 核心）
- [x] 7.3（後端）：規劃建議含來源依據，且 API 可穩定回應
- [x] 7.3（前端）：可直接在產品流程中上傳知識、檢視來源並套用建議
- [ ] 7.3（品質）：固定測資下生成結果可穩定滿足任務規劃需求

---

## Phase 8 - 後端抽象與交易邊界收斂（2026/05）

> **主旨：在不大幅改動產品功能的前提下，降低 service 層對 transaction、ORM entity 建構與 session 操作的耦合。**
> **策略：先收斂 commit/rollback，再抽 entity builder，Unit of Work 暫緩。**

### 8.1 | Transaction helper 與主線 commit/rollback 收斂 ✅ 完成（2026/05/15）

- [x] 新增 `backend/services/transactions.py`
- [x] 新增 `transaction(error_cls, error_message, status_code=500)`，集中 commit / rollback 與 operation error 保留規則
- [x] `knowledge_service.py` upload / reindex / delete / event 記錄流程改為明確交易段落
- [x] `task_service.py` 移除 `_commit_or_raise_task_error()`，任務更新、成員、留言、子任務、附件與建立任務流程改用 `with transaction(...)`
- [x] `timeline_service.py` 移除 `_commit_or_raise_timeline_error()`，專案更新、成員、風險通知與 AI 批次建任務流程改用 `with transaction(...)`
- [x] 主線 `knowledge_service.py` / `task_service.py` / `timeline_service.py` 已無直接散落 `db.session.commit()` / `db.session.rollback()`

### 8.2 | ORM entity builder 與 session helper 收斂 ✅ 完成（2026/05/16-05/17）

- [x] 新增 `backend/repositories/session_repository.py`，統一 `add_entity()` / `flush_session()` / `delete_entity()`
- [x] `task_repository.py` 新增 `build_task_entity()`、`build_task_member_entity()`、`build_notification_entity()`
- [x] `timeline_repository.py` 新增 `build_timeline_task_entity()`、`build_timeline_entity()`、`build_timeline_member_entity()`
- [x] `create_task_for_user()`、`batch_create_tasks_for_timeline()`、`create_timeline_for_user()` 改為使用 repository builder + session helper
- [x] 補強 task file 測試：確認上傳後實體檔案存在，刪除後 DB record 與檔案皆移除

### 8.3 | 一致性與防呆收斂 ✅ 完成（2026/05/17）

- [x] `group_members` 加入唯一性約束 migration，避免同使用者重覆加入同群組
- [x] `join_group_by_invite_code` 補 `IntegrityError -> 409`，高併發/重送請求可穩定回應
- [x] `auth_service` / `profile_service` email normalize 對齊（`strip().lower()`）
- [x] group snapshot jobs 增加 TTL + 最大保留筆數清理策略
- [x] trash 永久刪除流程改為先 DB 後檔案清理，交易語義一致
- [x] 聚焦回歸：`50 passed`（auth/profile/group/trash）+ `5 passed`（snapshot job）

### 8.3-doc | 文件與學習紀錄 ✅ 完成（2026/05/16）

- [x] 新增 `docs/後端Transaction重構順序_2026-05-15.md`
- [x] 新增 `docs/後端Transaction與ORM抽象說明_2026-05-16.md`
- [x] 將第九波 Unit of Work 留作後續讀到《Architecture Patterns with Python》相關章節後再評估

### 8.4 | 前端大元件維護性拆分 ✅ 核心完成（2026/05/27）

- [x] `TimelineDetailDialog.vue` 依職責拆分（Task 詳情 / Project Files / AI Planning）
- [x] `TimelineViewModes.vue` 拆分 list/card/kanban/calendar/gantt 與 kanban task modal 各視圖邏輯
- [x] 主元件改為編排層（props/emits），降低單檔責任密度
- [x] 既有前端回歸驗證通過（build + 聚焦 vitest）
- [x] 子元件獨立測試補齊（`TimelineSubcomponents.test.ts`，3 passed）

### 8.5 | 後端 service 複雜度收斂 ✅ 完成（2026/05/27）

- [x] `timeline_service.py` 主路徑 helper 化：`weekly_report` / `conflict_check` / `batch_create_tasks` 入口驗證與步驟拆分
- [x] 新增後端契約層（現已收斂為 `backend/contracts/`），建立 `timeline_contracts.py` + `task_contracts.py`
- [x] `task_service.py` 主路徑契約化：`create_task_for_user`、`update_task_for_member`、`update_task_status_for_member`
- [x] service 層驗證錯誤統一映射回 `TimelineOperationError` / `TaskOperationError`（維持既有錯誤語義）
- [x] 維持外部 API 契約與前端行為不變（僅內部結構整理）
- [x] 後端全量回歸通過：`194 passed`（僅 `.pytest_cache` 權限 warning）

### 8.6 | CI 最後一哩 ✅ 完成（2026/05/27）

- [x] Frontend GitHub Actions 收斂為純 CI：`npm ci` + `npm run test:coverage` + `npm run build`
- [x] 加入 `npm run guardrails:payload` 至 Frontend PR checks
- [x] 新增 `frontend-deploy.yml`，將 Deploy 與 PR 測試流程拆分
- [x] 補齊 `docs/guides/CI_CD_最小流程.md`（提交規範、PR merge gate、回滾）
- [x] 新增 `.github/pull_request_template.md`（風險/測試證據/回滾方式）
- [ ] GitHub Branch Protection（`main` required checks、禁止直接 push）需在 GitHub UI 手動設定

### 8.7 | 工程護欄補強 ✅ 完成（單人精簡版，2026/05/28）

- [x] 增加 PR Template（風險、測試證據、回滾方式）
- [x] 固定關鍵路徑 smoke test 指令（前端/後端各一組，見 `docs/Phase8_7_工程護欄補強計畫_2026-05-27.md`）
- [x] 規範「拆分步驟 -> 測試證據 -> 文件同步」提交流程（單人版）
- [x] 高風險檔案 checkpoint 策略（單人版：feature branch + 波段 commit + stash）

### 驗收門檻

- [x] 不改 API 契約與前端行為
- [x] 主線回歸通過：`43 passed`
- [x] Service 層 transaction 邊界更清楚，repository / session helper 負責更集中的 persistence 細節
- [ ] 後續若導入 UoW，需先建立清楚的 repository interface / fake repository 測試策略

---

## Phase 9 - 型別註解、Docstring 與 Tool Calling（2026/06）

> **主旨：在不改變既有功能的前提下，提升 Python 程式可讀性、可推導性與工具調用穩定性。**

### 9.1 | Type Hints 基線（型別註解）

- [x] 先覆蓋 `repositories/`、`services/transactions.py`、`blueprints/validation.py`
- [x] 再擴展到 `task_service.py` / `timeline_service.py` 核心入口函式
- [x] 建立主要輸入/輸出型別別名，避免同型別重複宣告（以現有契約 model + `dict[str, Any]` 邊界型別先行）
- [x] 收斂剩餘 `Any` 邊界第一波（優先處理 ORM query row / 動態 payload；`group_repository` query rows 已改為 `TypedDict`）

### 9.2 | Docstring 規範化（功能描述註解）

- [x] 統一 docstring 風格（Google style）
- [x] 針對 service 對外函式補齊：用途、輸入、回傳、錯誤情境
- [x] 避免流水帳描述，改寫成「行為契約 + 邊界」

### 9.3 | 單體後端 Tool Calling 友善化（先不走 MCP）

- [x] 建立單體版 tool registry（只暴露 service 穩定入口，不直接暴露 helper）
- [x] LangGraph 先直連後端 tool registry，完成最小閉環（輸入需求 -> 工具調用 -> 回傳）
- [x] 固定工具層參數 schema 與回傳結構，提升可驗證性
- [x] 建立最小 smoke 測試，驗證工具調用契約未破壞
- [ ] （Backlog）MCP 串接延後：保留待辦，不納入當前 9.3 交付範圍

### 9.4 | Agent 雙階段執行（Plan -> Confirm -> Execute）

- [x] 導入 `plan -> confirm -> execute` 雙階段流程（先規劃，不直接寫入）
- [x] 新增 `POST /api/copilot/agent/plan`（回傳 plan 摘要、步驟預覽、風險提示、TTL）
- [x] 新增 `POST /api/copilot/agent/reject` 與 `POST /api/copilot/agent/replan`
- [x] `POST /api/copilot/agent/execute` 支援 `plan_id + confirm=true` 執行已核准計畫
- [x] 新增 in-memory plan store（狀態：`planned/executing/succeeded/failed/rejected/expired`）
- [x] execute 階段鎖定核准步驟快照，避免執行期任意偏離
- [x] 前端 `CopilotDock` 改為兩段式 UX：先看計畫摘要，再確認執行
- [x] 前端隱藏內部 ID 細節，僅顯示使用者可理解資訊
- [x] 補最小測試：plan/execute/reject 服務流程與 registry 相容性
- [ ] （Backlog）跨系統補償回滾（Saga/undo）與 MCP 遠端一致性策略

### 9.5 | 模型提案式 Plan（規則保留）與 Replan 強制二次確認

- [x] 將 `plan` 升級為「模型提案主路徑 + `build_pending_tools` fallback」
- [x] 模型提案輸出固定 JSON 契約（`supported/steps/payload_draft/reason`）
- [x] 後端對提案做白名單與 schema 驗證（非法工具/欄位直接拒絕）
- [x] `replan` 強制重新餵 `tool list + 註解 + schema` 給模型重提案
- [x] `replan` 一律產生新 `plan_id`，且必須二次 `confirm`
- [x] 在 plan UI 顯示提案來源（LLM / fallback）與提案理由
- [x] 新增 9.5 測試：LLM 命中、fallback 行為、force-model 失敗保護
- [x] 測試啟動基線修正：`backend/pytest.ini` 補 `pythonpath = .`，`langgraph` 已安裝到測試環境
- [x] 規劃文件：`docs/Phase9_5_模型提案式Plan與Replan強制二次確認規劃_2026-06-02.md`

### 9.5+ | Agent 契約一致性與安全邊界補強（2026/06/05）✅ 完成

- [x] 對齊 `plan / replan / execute` 的 `tool_payloads` 契約
  - `CopilotAgentPlanPayload` 補 `tool_payloads`
  - `CopilotAgentReplanPayload` 補 `tool_payloads`
  - `CopilotAgentExecuteByPlanPayload` 移除 `tool_payloads`
  - `/agent/replan` 後端接收並傳入 `tool_payloads`
- [x] `execute_registered_tool()` 補 handler exception boundary，統一回 failure envelope
- [x] `agent_nodes.py` 對 `steps` / `step` 改為防禦式讀取，降低 malformed state 直接 500 風險
- [x] `tool_plan_service.py` prompt 補 protected keys 規則，避免模型在 `payload_draft` 亂填 scope 欄位
- [x] planner steps 設定上限，避免 plan preview 與 execute `max_loops` 語意不一致
- [x] `ToolSuccess / ToolFailure.ok` 收斂為 `Literal[True/False]`
- [x] `TaskCreateInput` 的 list default 改為 `Field(default_factory=list)`
- [x] `create_notification()` 失敗時補 warning log，不再完全吞掉例外
- [x] 補測：
  - replan 保留 `tool_payloads`
  - execute 拒絕 `tool_payloads` override
  - registry 可攔住 handler exception
  - malformed step 不會直接 500
- [x] 第一波 service 權限邊界補洞：task / timeline 更新主線改由 service 內 `operator_user_id` 驗證，不再只依賴 blueprint guard
- [x] 規劃文件：`docs/phases/Phase9_5_Agent契約一致性與安全邊界補強計畫_2026-06-05.md`

### 9.5++ | Agent 規劃與執行體驗收斂（2026/06/07）✅ 完成

- [x] `tool_plan_service.py` 補 LLM content block 正規化，允許模型回傳 list-style content 而不再因 `.strip()` 直接 500
- [x] planner 輸出補 `planning_mode`，並由模型明確區分 `create_project_only / plan_tasks_only / plan_and_create_tasks`
- [x] `registry.py` 補 `planner_role / workflow_group / completes_after` metadata，讓 planner 能以 suggestion/apply 語意規劃流程
- [x] `copilot_service.py` 補 plan / execute 可觀測欄位（`pending_tools`、`approved_pending_tools`、`executed_tools`）與 logging
- [x] `agent_nodes.py` 補 partial payload merge、project name 推導、`created_timeline_name` 傳遞、generated tasks fallback 與成功摘要組裝
- [x] `CopilotDock.vue` 顯示工具步驟摘要與 AI 生成任務清單，不再只剩固定完成訊息
- [x] 新增 `docs/future/` 目錄與第一份 backlog 草稿：`Future_AgentPrompt分域與Planner收斂方向_2026-06-07.md`
- [x] 驗證：
  - `pytest backend/tests/chains/test_agent_graph.py -q` → `17 passed`
  - `pytest backend/tests/services/test_copilot_plan_flow.py backend/tests/services/test_tool_plan_service.py -q` → `16 passed`
  - `py_compile backend/chains/agent_nodes.py` → PASS
- [ ] 後續待補：前端 `Vitest` 重新驗證 `CopilotDock` 顯示路徑（當前本機環境先前曾遇到 `spawn EPERM`）

### 9.6 | Agent Trace-only 前置項

> **定位調整：完整 evaluation / benchmark / gate 延後到 Phase 12，完整 observability 收斂延後到 Phase 13；9.6 目前只保留最小 trace 基線，作為進入後續 phase 的前置準備。**

- [ ] 建立 plan/execute/replan trace（steps、error_code、route、耗時）
- [ ] 補 request_id 與 plan_id 串接，支援一次操作完整追蹤
- [ ] 定義最小 trace 欄位集合，避免後續 logging 名稱漂移
- [ ] 明確延後：
  - benchmark 題集、runner、報表模板 → `Phase 12 - Evaluation`
  - error/metrics 收斂與可觀測性驗收 → `Phase 13 - Observability`
- [ ] 規劃文件：`docs/phases/Phase9_6_Agent可觀測性與評測基線規劃_2026-06-02.md`

---

## Phase 10 - 契約收斂與前後端對齊（2026/06）

> **主旨：把整個系統的後端 schema、前端 type、request/response、agent/tool payload 與閱讀版文件收斂成同一套契約語言，降低開發時前後端鬆散、欄位漂移與驗證散落的成本。**  
> **定位：這一階段是面向整個系統的前後端契約維護 phase；agent 只是其中一個高風險模組，不是唯一主軸。**  
> **完成定義：Phase 10 做完時，系統內主要契約面應已完成對齊，並且高風險欄位、鬆散 response shape、受保護欄位邊界與文件真相來源都已收斂到可穩定維護的程度。**

- [x] 規劃文件：`docs/phases/Phase10_契約收斂與前後端對齊規劃_2026-06-12.md`

### 10.1 | 全系統契約盤點基線

- [x] 補一份 10.1 規劃文件
  - 文件：`docs/phases/Phase10_1_全系統契約盤點基線規劃_2026-06-12.md`
- [x] 建立系統模組契約地圖
  - auth / profile
  - tasks
  - timelines
  - knowledge
  - groups / messages / notifications
  - copilot / agent
- [x] 標出每個模組的主要契約入口
  - blueprint route
  - frontend 呼叫點
  - 對應 type / schema / tests
- [x] 建立第一版高風險 API 清單
  - 以「容易前後端不一致」與「經常改動」優先
- [x] 先修第一批明顯不對齊的 frontend service / type
  - `tasks` 子任務 response envelope 對齊
  - `task tags` 字串 -> 陣列正規化
  - `groups` create/join/leave/sendMessage response shape 對齊
  - `notifications` / `trash` / `todos` / `timelines` mutation response 型別對齊
  - `copilot` 前端型別補 `request_id` / `trace`

### 10.2 | 後端契約來源收斂

- [x] 補一份 10.2 規劃文件
  - 文件：`docs/phases/Phase10_2_後端契約來源收斂規劃_2026-06-12.md`
- [x] 補一份後端契約來源索引
  - 文件：`docs/reference/backend_契約來源索引.md`
- [x] 盤點目前驗證散落位置
  - blueprint 內 `Pydantic` payload
  - 共用 validation helper
  - tool contract schema
  - service 內 guard
- [x] 盤點 response shape 來源（第一版）
  - blueprint 直接 `jsonify`
  - service 回傳 envelope
  - tool contract envelope
- [x] 定義「欄位契約」與「業務規則」邊界
  - 哪些應抽成共用 schema
  - 哪些應留在 service / domain guard
  - 哪些欄位只能後端決定（protected keys）
- [x] 產出第一版後端契約目錄策略
  - request / response schema 放哪裡
  - tool / agent contract 放哪裡
  - error envelope 與共用欄位放哪裡
  - 目標結構已正式定為 `backend/contracts/` 作為後端共用契約層
  - blueprint / service / tool 共用契約已自 `backend/services/contracts/` 正式搬遷到 `backend/contracts/`
- [x] 第一輪後端 request contract 收斂落地
  - 新增：`shared_fields.py`、`auth_contracts.py`、`profile_contracts.py`、`knowledge_contracts.py`、`todo_contracts.py`、`group_contracts.py`
  - 延伸調整：`task_contracts.py`、`timeline_contracts.py`、`tool_inputs.py`
  - blueprint 已接上集中 contract：`auth`、`profile`、`tasks`、`timelines`、`knowledge`、`todos`、`groups`
  - service 主路徑已開始共用 contract：`auth_service`、`profile_service`、`task_service`、`timeline_service`、`todo_service`、`group_service`
  - 補一份實作整理：`docs/reference/Phase10_2_後端契約收斂實作整理_2026-06-15.md`
  - 驗證：聚焦 pytest `83 passed`、擴大聚焦 pytest `137 passed`、`py_compile` PASS
- [x] 契約層目錄正式定版為 `backend/contracts/`
  - `backend/services/contracts/` 已完成搬遷
  - backend import、README、索引與 Phase 10 文件已同步對齊
- [ ] response contract 第二輪收斂
  - mutation / listing / analysis response 命名與 envelope 還未全面統一
  - 這部分留給 10.3 / 10.4 接續，不在 10.2 這輪硬改所有回應 shape

### 10.3 | 前後端契約對齊

- [x] 先做全系統主要模組 backend response contract 收斂
  - 先盤點主要模組 response 真相來源
    - `tasks`
    - `timelines`
    - `knowledge`
    - `copilot / agent`
    - `auth / profile`
    - `groups / messages / notifications`
    - `todos / trash`
  - 區分目前 response 類型
    - mutation response
    - listing response
    - analysis / AI response
    - tool envelope
  - 標出哪些 response 仍由 blueprint 直接承諾
  - 標出哪些 response 已由 service payload / tool envelope 承諾
  - 這一輪直接完成主要模組 response contract 統一
    - mutation key / envelope 統一
    - listing response 外型保留既有裸陣列或 service payload，不破壞 frontend consumer
    - analysis / AI response 固定由 service payload 承諾，不再由 blueprint 臨時拼接
    - tool envelope 維持一致
- [x] 補一份 10.3 後端 response contract 實作整理
  - 文件：`docs/reference/Phase10_3_後端回應契約收斂實作整理_2026-06-17.md`
  - 明確標出已收進 `response_contracts.py` 與仍由 service payload 承諾的 response
- [x] tasks service payload response contract 補強
  - `task_contracts.py` 新增 task list/member/comment/file/subtask/summary response models
  - `task_service.py` serializer / payload builder 已接上 contract validation
  - 保留既有 frontend response shape，不改成新 envelope
- [x] timelines service payload response contract 補強
  - `timeline_contracts.py` 新增 timeline list/task/member/batch/upcoming/member-stats response models
  - `timeline_service.py` 穩定 serializer / payload builder 已接上 contract validation
  - weekly report / risk analysis / conflict / AI 類 payload 先保留 service 承諾，不硬抽成完整 schema
- [x] knowledge service payload response contract 補強
  - `knowledge_contracts.py` 新增 document/list/action/batch/event response models
  - `knowledge_service.py` 主要 JSON payload 已接上 contract validation
  - download / preview blob response 不納入 JSON response contract
- [x] groups service payload response contract 補強
  - `group_contracts.py` 新增 group list/member/message/snapshot/job response models
  - `group_service.py` list/member/message/snapshot/job status payload 已接上 contract validation
  - snapshot summary 內容保留 dict 彈性，先驗證外層穩定欄位
- [x] 盤點前端目前直接依賴的第一批主要欄位
  - auth / profile
  - task
  - timeline
  - knowledge
  - groups / messages / notifications
  - upcoming
- [x] 對照前端 type 與後端 request/response 的第一輪漂移
  - 哪些欄位名稱不一致
  - 哪些欄位 optional / required 判定不同
  - 哪些欄位前端假設存在、後端其實不保證
  - 哪些是 request 已收斂、但 response 還未穩定
- [x] 建立第一批「已對齊 / 待對齊」契約清單
  - 覆蓋主要模組 response contract 與 frontend consumer
  - 已先收斂 auth / profile / upcoming / groups snapshot 幾個最明顯 consumer
- [x] 針對主要模組 API 補 focused 驗證
  - backend response shape 測試
  - 新增 `backend/tests/services/test_response_contracts.py`
  - focused pytest：`85 passed`（僅 `.pytest_cache` 權限 warning）
  - task service payload contract focused pytest：`43 passed`（僅 `.pytest_cache` 權限 warning）
  - timeline service payload contract focused pytest：`44 passed`（僅 `.pytest_cache` 權限 warning）
  - knowledge service payload contract focused pytest：`14 passed`（僅 `.pytest_cache` 權限 warning）
  - groups service payload contract focused pytest：`16 passed`（僅 `.pytest_cache` 權限 warning）
  - task frontend focused test：`4 passed`
  - timeline frontend focused test：`15 passed`
  - knowledge frontend focused test：`8 passed`
  - groups frontend focused test：`15 passed`
  - frontend service/type focused test：`32 passed`
  - frontend build：PASS（僅既有 chunk size warning）
- [x] 產出第一版 OpenAPI 文件或可生成 OpenAPI 的輸出入口
  - OpenAPI 作為從 `backend/contracts`、route、response contract 與測試長出的輸出層
  - 用於閱讀、檢查、前後端對齊與後續工具化
  - 已新增 `backend/openapi_document.py` 與 `/api/openapi.json`
  - 目前為第一版手寫輸出入口，後續 10.4 再補維護流程與 Swagger / schema export 策略
  - 不讓 OpenAPI 取代程式內契約真相來源
- [x] 補剩餘 frontend consumer 對齊
  - 續掃 `copilot / agent` 外層 response、主要 runtime consumer 與殘留 cast / shape guard 後，10.3 主線範圍已收斂完成
  - 以 `docs/reference/openapi.json` 與 `backend/contracts/` 為準，已補第一批 `copilot / agent` 外層型別與 `auth/profile/groups` 幾個殘留 cast cleanup
  - 已補共用 JSON 型別、`api.ts` refresh typing、shape guard 與主要 consumer cleanup
  - 剩餘動態 payload 與少數非核心頁面細修，改列 10.4+ 維護，不再視為 10.3 阻塞
- [x] 補一份 protected fields / frontend 不可自送欄位說明
  - 文件：`docs/reference/Phase10_3_protected_fields_與_frontend_不可自送欄位說明_2026-06-17.md`
  - 明確整理 client-owned fields、server-owned / protected fields、route-owned identifiers 與 frontend / backend review 檢查點

### 10.4 | 契約文件與輸出層準備

- [ ] 補一份閱讀版契約索引
  - 哪些 API 已有穩定 schema
  - 哪些 API 已有穩定 response contract
  - 哪些 API 已能反映到 OpenAPI 文件或輸出入口
  - 哪些仍在重構期
  - 哪些已有 focused tests 守護
- [ ] 定義文件維護原則
  - 文件要貼近後端 schema，不再另外手寫一套真相
  - 變更主要 API 時，同步檢查前端 type、response shape、OpenAPI 輸出與契約文件
- [ ] 收斂 OpenAPI 文件維護流程
  - 10.3 先產出第一版 OpenAPI 文件或輸出入口
  - 10.4 再補文件維護原則、缺口標記與後續 Swagger UI / 契約匯出策略
  - 契約層目錄已正式定為 `backend/contracts`；後續 10.3 / 10.4 直接以此作為後端真相來源

### 10.5 | 模組別導覽與不穩區塊盤點

- [ ] 補一份 tasks 主線導覽
  - 頁面入口、type、API、service、測試對應
- [ ] 補一份 timelines 主線導覽
  - 頁面入口、type、API、service、測試對應
- [ ] 補一份 knowledge 主線導覽
  - 個人知識庫、project files、reindex / preview / delete
- [ ] 補一份 groups / messages / notifications 主線導覽
  - 主要事件流與欄位依賴
- [ ] 補一份 copilot 主線導覽
  - 前端入口、agent API、tool planning、execute / summarize
- [ ] 補一份「目前最不穩區塊」清單
  - 只聚焦：測試、契約、回傳形狀、欄位漂移、agent flow
  - 不擴成全 repo 全面重學

### 10.6 | 全系統收尾與鬆動點清理

- [ ] 建立主要模組的契約差異表
  - `tasks`
  - `timelines`
  - `groups`
  - `knowledge`
  - `copilot / agent`
- [ ] 收掉仍存在的高風險鬆動點
  - route 回傳 shape 與 frontend service type 不一致
  - payload 仍混用 entity model / DTO model
  - protected fields 邊界仍靠慣例而非明確說明
  - response envelope 未標準化的 mutation API
- [ ] 確認主要模組已達到「可穩定維護」狀態
  - 前端不再靠 cast 或猜測消費高風險 response
  - 後端 request / response 真相來源可快速定位
  - 閱讀版文件與實作不再長期漂移
- [ ] 若前面階段未足以完成收斂，可持續追加 `10.6.x` 或 `10.7+`
  - 本 phase 允許延長收尾段，不強迫被固定分段卡住

### 10.7 | 執行護欄與收斂順序

- [ ] 每次處理一條契約主線時，固定先做契約比對
  - 先找 backend route / schema / response
  - 再找 frontend service / type / store / component
  - 最後記錄差異與修正方向
- [ ] 每次修契約時，固定先修真相來源，再修消費端
  - 先修 backend schema / response contract / protected fields
  - 再修 frontend type / mapper / service
  - 最後補閱讀版文件或契約索引
- [ ] 每次遇到欄位規則時，先判斷它屬於哪一層
  - 欄位格式與 request/response shape 留在 schema contract
  - 權限、流程限制、商業判斷留在 service / domain guard
  - 不把不同層責任混進同一份型別或同一個 helper
- [ ] 每次處理高風險 API 時，至少同步檢查四個點
  - backend request schema
  - backend response shape
  - frontend type / payload
  - focused tests 或契約差異表
- [ ] 每完成一批契約修正時，回頭更新模組狀態
  - 標記哪些模組已對齊
  - 標記哪些模組仍有鬆動點
  - 若還有高風險殘留，直接續開 `10.6.x` / `10.7+` 收尾，不強迫進下一 phase

---

## Phase 11 - Integration Test（2026/06-07）

> **主旨：補齊跨層流程驗證，讓專案不只單點函式可測，而是整條使用者路徑可驗。**

### 11.1 | Agent 主流程整合測試

- [ ] 覆蓋 `plan -> confirm -> execute -> summarize` 主路徑
- [ ] 覆蓋 `replan -> confirm -> execute` 路徑
- [ ] 覆蓋 fallback / refusal / malformed proposal / retryable error 路徑
- [ ] 覆蓋高副作用工具遇到 failure 時不自動重試的保護

### 11.2 | Timeline / Knowledge / Copilot 整合測試

- [ ] 覆蓋 timeline AI 生成任務 -> batch create -> refresh 主線
- [ ] 覆蓋 knowledge 文件上傳 / reindex / project scope / agent read path
- [ ] 覆蓋 project member / actor guard 對 tool path 的保護
- [ ] 優先測「真正常壞掉的跨層流程」，不是單純補測試數量

### 11.3 | 測試策略

- [ ] 為 integration tests 明確定義目錄與命名規則
- [ ] 保持 fixture / factory 可重用，避免測試一長就再次變史山
- [ ] 讓失敗訊息能快速指出是 contract、permission、planner 還是 handler 問題
- [ ] 每條整合測試至少對應一條實際使用者流程，不做純湊數測試

---

## Phase 12 - Evaluation（2026/06-07）

> **主旨：把 Agent / AI / RAG 從「感覺可用」提升成「能量化比較」。**

### 12.1 | Benchmark 題集

- [ ] 建立固定 benchmark 題集，至少涵蓋：
  - timeline 建立 / 任務規劃
  - task update / conflict-check
  - knowledge-assisted planning
  - replan / reject / refusal
- [ ] 題集需同時包含成功案例、拒絕案例與錯誤案例
- [ ] 每題補「預期工具路徑 / 預期結果 / 驗收條件」

### 12.2 | 評測指標

- [ ] 成功率（是否走到正確工具與正確結果）
- [ ] 步驟數（是否過度繞路）
- [ ] fallback 命中率
- [ ] error taxonomy（失敗主要落在哪一層）
- [ ] latency / 平均耗時

### 12.3 | 評測輸出

- [ ] 補 benchmark runner
- [ ] 補簡單報表模板（表格即可，不先追求儀表板）
- [ ] 建立最小 gate，讓後續改 agent/prompt 時有回歸基線
- [ ] 讓報表能回看「哪一類題目最常失敗」

---

## Phase 13 - Observability（2026/06-07）

> **主旨：讓一次 agent 操作可追、可看、可定位，而不是只知道「失敗了」。**

### 13.1 | Trace 基線

- [ ] 為 plan / replan / execute 建立一致 trace 結構
- [ ] 串 `request_id`、`plan_id`、`tool_name`、`error_code`
- [ ] 補 step-level logging，能看出模型提案、schema reject、tool execute 的分界
- [ ] 補一份 trace 欄位定義表，避免後續 logging 欄位名稱漂移

### 13.2 | Error 與 Metrics

- [ ] 收斂錯誤分類：planner / contract / permission / handler / external provider
- [ ] 最小 metrics：成功數、失敗數、平均耗時、fallback 次數
- [ ] 先以本地/後端可讀報表為主，不急著先做完整視覺 dashboard
- [ ] 至少能用一個命令或一份報表看出最近一批 benchmark 的錯誤分布

### 13.3 | 驗收標準

- [ ] 能對任一筆 agent 操作回答：
  - 收到什麼輸入
  - 模型提了什麼 plan
  - 哪一步失敗
  - 為什麼失敗
  - 是否 fallback
  - 最終是否有 side effect

---

## 功能方向收斂（11~13 之後才考慮）

> **原則：只做強化既有核心域的功能，不再往泛協作平台膨脹。**

### 可考慮的功能（依優先順序）

1. `Milestone`
2. `Weekly Review`
3. `Workload` 深化
4. `Global Search`
5. `Activity Feed`

### 暫不做的功能

- 聊天室 2.0
- 筆記模組
- 文件模組
- 白板模組

### 不做原因

- 容易把專案拉成 Notion clone / 泛協作平台
- 功能面會變胖，但對當前工程能力成長與系統主線價值有限
- 會稀釋 timeline / task / knowledge / agent 這條最有辨識度的核心域

---

## 附錄 A - 技術棧

### 前端棧（已實際引入 ✅）

| 層級 | 技術 | 版本 | 用途 |
|------|------|----|------|
| **框架** | Vue 3 | 3.5.17 | Progressive framework |
| **路由** | Vue Router | 4.6.4 | SPA 路由管理 |
| **狀態管理** | Pinia | 3.0.4 | 組件狀態管理 |
| **HTTP** | Axios | 1.13.2 | API 請求 |
| **樣式** | Tailwind CSS | 4.1.18 | Utility-first CSS |
| **樣式建置插件** | @tailwindcss/vite | 4.1.18 | Vite 直接整合 Tailwind |
| **構建工具** | Vite | 7.2.4 | 現代化打包器 |
| **型別系統** | TypeScript | 5.9.3 | 型別安全（Phase 3 已遷移）|
| **UI 元件** | Headless UI | 1.7.23 | 無樣式基礎元件 |
| **Toast 通知** | vue-sonner | 2.0.9 | 輕量通知框架 |
| **實時通訊** | Socket.IO Client | 4.8.1 | WebSocket 即時聊天（Phase 4.11 完成）|
| **拖拽排序** | vuedraggable | 4.1.0 | 看板拖曳排序 |
| **甘特圖** | frappe-gantt | 1.2.2 | 任務時間軸視圖 |
| **日期選擇** | FullCalendar | 6.1.20 | 專案月曆視圖（含 multimonth/daygrid） |
| **數據圖表** | ECharts / vue-echarts | 6.0.0 / 8.0.1 | Phase 2 統計儀表板（已完成）|
| **雲服務** | Firebase | 12.11.0 | Phase 5 Hosting + Auth |

### 後端棧（已實際引入 ✅）

| 層級 | 技術 | 版本 | 用途 |
|------|------|----|------|
| **框架** | Flask | 3.x | Web 框架 |
| **ORM** | SQLAlchemy | Flask-SQLAlchemy | 資料庫 ORM |
| **遷移** | Alembic | Flask-Migrate | Schema 版本管理 |
| **認證** | JWT | Flask-JWT-Extended | Token 認證（含 refresh）|
| **CORS** | Flask-CORS | 4.x | 跨域資源共享 |
| **實時通訊** | Socket.IO | Flask-SocketIO 5.x | WebSocket 雙向即時通訊（Phase 4.11 完成）|
| **AI 應用框架** | LangChain + LangChain Core | backend/requirements 主線 | Prompt / Runnable / OutputParser 流程 |
| **Workflow 編排** | LangGraph | backend/requirements 主線 | AI 工作流與狀態機（Phase 6.6+） |
| **圖演算法** | NetworkX | >=3.0 | Critical Path DAG 分析與風險分級（Phase 7.2） |
| **驗證層** | Pydantic | >=2.0 | 結構化輸出驗證（含 ConfigDict） |
| **模型提供者** | Google Gemini | langchain-google-genai | AI 任務生成與摘要主線 |
| **MCP** | mcp (Python SDK) | >=1.0.0 | MCP 工具註冊與 stdio/JSON-RPC 溝通 |
| **環境配置** | python-dotenv | 1.x | .env 檔案管理 |
| **資料庫** | PostgreSQL | pg8000 1.29.7 | Phase 5 生產資料庫（Supabase）+ 本地主線 |
| **部署** | Gunicorn + Eventlet | 21.2.0 / 0.33.3 | 生產 WSGI + Socket 長連線支援 |
| **部署平台** | Railway (Nixpacks) | 平台代管 | 後端自動部署與環境託管 |

### 測試與品質工具（已實際引入 ✅）

| 工具 | 版本 | 用途 |
|------|------|------|
| Vitest | 4.1.2 | 前端單元測試（Phase 5.5 完成：85 tests, 88.85% coverage）|
| @vitest/coverage-v8 | 4.1.2 | v8 代碼覆蓋率分析 |
| @vitest/ui | 4.1.2 | Vitest 互動式測試後台 |
| @vue/test-utils | 2.4.6 | Vue 元件測試工具 |
| jsdom | 29.0.1 | DOM 環境模擬 |
| Stryker | 9.6.0 | Phase 6.1 / Labs 變異測試（353 utils mutants）|
| @stryker-mutator/vitest-runner | 9.6.0 | Vitest 集成 |
| @stryker-mutator/typescript-checker | 9.6.0 | TypeScript 編譯檢查 |
| pytest / pytest-cov / pytest-flask | backend requirements 主線 | 後端單元測試、覆蓋率與 Flask 測試夾具 |

---

## 附錄 B - 不納入項目
- ❌ 深色模式（side project 暫不考慮）

---


## 附錄 C - 歷史記錄

### 2026/01/22 進度
- 完成「AI 任務生成」功能，整合 Google Generative AI (Gemini 2.5 Flash Lite)
- 完成「軟刪除」(Soft Delete) 機制
- Alembic migration 衝突解決
- .gitignore 檢查完成

### 2026/02/19 發現與修正
- 發現缺少 Token 刷新機制
- 修正 `window.location.href` → `router.push()`（SPA 正確做法）

### 2026/02/21 完成
- 後端新增 `/auth/refresh` endpoint
- 前端 Response 攔截器實作完整的 token 自動刷新機制（含 Queue 防競態）
- 提取 `BASE_URL` 常數避免重複定義

### 2026/02/24~25 Phase 0 Step 0-1, 0-2 完成
- **Step 0-1 完成**：建立 5 個 service 檔案（todo、task、group、profile、timeline）
- 所有 View 移除直接 `api.` 呼叫，改用 service 層
- 修正 `Sidebar.vue` fragment 根節點警告（加 wrapper div + defineEmits）
- 修正 `TasksView.vue` 日期格式問題（`.slice(0, 10)`）
- 修正 `tasks.py` assistant 欄位 array → string 轉換
- 修正 `timelines.py` task_comments SQL 欄位名稱錯誤
- `TasksView.vue` 表單改為 Modal（Teleport）
- `TimelinesView.vue` 硬編碼 URL 改為 `apiBaseUrl` 變數
- 原估 3 天，實際 1 天（Vibe Coding 加速）
- **Step 0-2 完成**：建立 todos 和 tasks store
- `stores/todos.js` — 完成 CRUD、computed filters（無 loading）
- `stores/tasks.js` — 完成 CRUD、subtasks 操作（有 loading，供 TimelinesView 使用）
- TodosView、TasksView 改用 store，測試通過
- 修正 datetime-local 時區問題、storeToRefs 響應式問題
- 原估 3～5 天，實際 1 天

### 2026/02/25 Phase 0 Step 0-3 完成
- **Step 0-3 完成**：TimelinesView.vue 第一層拆分
- `TimelinesView.vue` 2569 行 → **301 行**（降幅 88%，遠超目標 1200 行）
- 新增 `components/timelines/TimelineHeader.vue`（125 行）：Header + Stats + ViewToggle
- 新增 `components/timelines/TimelineViewModes.vue`（741 行）：4 種視圖 + 看板任務 Modal
- 新增 `components/timelines/TimelineDetailDialog.vue`（405 行）：專案詳情 + 任務所有 Modal
- 子元件直接呼叫 service（Phase 0 原則：不新增 timelines store），透過 emit 通知父層刷新
- 修正 TimelineDetailDialog 的 service 方法呼叫（使用正確的 timelineService 方法）
- 舊檔案備份為 `TimelinesView.vue.bak`（共 2569 行）
- Vite 編譯通過：136 模組無錯誤
- 原估 1～2 天，實際 1 天

### 2026/02/26~27 Phase 1 多人協作系統完成
- **後端（timelines.py）全面重寫：**
  - 新增 `get_user_timeline_role()` helper（全面 ORM 化，移除所有 raw SQL）
  - 新增 `require_timeline_role('owner'|'member')` decorator factory，所有路由套上正確權限
  - 建立 timeline 後自動寫入 `timeline_users`（role=0 負責人）
  - `get_timelines` 改 JOIN `timeline_users`，每筆 timeline 含 `role` 欄位
  - 新增 `DELETE /<id>/members/<member_user_id>` endpoint
  - 移除 6 處無用 `user_id = int(get_jwt_identity())` 宣告
- **前端 role-based UI：**
  - `TimelineViewModes.vue` — 編輯/刪除按鈕加 `v-if="timeline.role === 0"`（List + Card 共 4 處）
  - `TimelineDetailDialog.vue` — 成員管理按鈕加 `v-if="selectedTimeline?.role === 0"`
  - `timelineService.js` 新增 `removeMember`、`getMembers`
  - 成員管理面板 UI — 顯示成員列表 + 踢人按鈕
- **tasks.py 安全修補：**
  - 新增 `require_task_role()` decorator（含 timeline 角色繼承）
  - 14 個路由加成員驗證，delete_task 等改為 owner-only
  - 前端任務成員管理 UI（取代舊 `assistant` 欄位文字輸入）
- 舊資料補填：`backfill_timeline_users.py` 補 5 筆，1 筆已存在跳過
- 原估 4～5 天，實際 2 天（2026/02/27 超前完成）

### 2026/03/01~02 Phase 1 任務留言、附件上傳、垃圾桶機制完成
- **任務留言與附件上傳：**
  - 後端 `tasks.py` 新增留言 CRUD（get/add/delete）+ 附件 CRUD（upload/get/delete/download）
  - 附件儲存於 `uploads/task_files/`，DB 保留 `original_filename`（解決 `secure_filename` 過濾中文的問題）
  - 下載改用 fetch+blob（解決跨來源 `<a download>` 無效問題）
  - 重複路由整合：`timelines.py` 移除 6 條與 `tasks.py` 重複的路由
  - 前端 `taskService.js`、`timelineService.js` 補完所有留言 + 附件方法
  - `TimelineDetailDialog.vue` 與 `TasksView.vue` 任務詳情 Modal 加入留言 + 附件 UI
  - 所有 model 補上 `cascade='all, delete-orphan'`（TaskComment、TaskUser、Subtask、TaskFile、TimelineUser）
- **垃圾桶機制：**
  - 後端新建 `trash.py` blueprint：`GET /api/trash`、`PATCH /restore`、`DELETE`（任務 + 專案各一套）
  - 永久刪除專案時同步清除所有子任務、附件（cascade + `os.remove`）
  - 前端 `trashService.js`（5 個方法）
  - `TrashView.vue`：已刪專案 + 已刪任務兩區塊，還原 / 永久刪除按鈕，非建立者唯讀
  - Router 新增 `/trash`、Sidebar 新增 🗑️ 垃圾桶入口
  - 修正：刪除專案後重新 call API（cascade 同步刪子任務，前端無法自行推算）
- 原估 3～4 天，實際 1.5 天

### 2026/03/02 Phase 1 收尾技術債完成
- **Store 層補完（timelines / groups / profile）：**
  - `stores/timelines.js`：timelines/allTasks/loading 狀態 + urgentCount/totalCompletedTasks/totalTasks/sortedTimelines computed + 全部 CRUD + getDaysRemaining 工具函式
  - `stores/groups.js`：groups/currentGroup/messages/loading 狀態 + scrollCallback 機制（scroll DOM 事件留在 View，store 透過 callback 觸發）
  - `stores/profile.js`：profile/stats/loading 狀態 + statCards computed
  - `TimelinesView.vue`、`GroupsView.vue`、`ProfileView.vue` 改用對應 store
  - 修正 bug：`createGroup` 回傳 `response.data` 物件，View 改用 `data.invite_code`
  - 修正 bug：`sendMessage(content, scrollCallback)` — groupId 由 store 內部從 `currentGroup` 取得
  - 清理 dead code：`ProfileView.vue` 移除未使用的 `stats` storeToRefs 解構；`TimelinesView.vue` 移除未使用的 fetchTimelines/fetchAllTasks wrapper
- **Subtask 前端 UI 補完：**
  - `TasksView.vue` 任務詳情 Modal：進度條 + 子任務列表（toggle/delete）+ 新增輸入，`openTaskDetail` 改 3 路 `Promise.allSettled`（comments + files + subtasks）
  - `TimelineDetailDialog.vue` 任務詳情：同上模式，風格與看板 Modal 一致
- **Phase 1 全部完成，提前 Phase 2 Stage**（原目標 03/05，實際 03/02）
- 原估 1～2 天，實際 半天

### 2026/03/04 Phase 2 前置整理
- 抽取 `frontend/src/utils/formatters.js`，統一全站日期與檔案格式化工具，減少 View 內重複函式
- 安裝 Phase 2 所需前端套件，為圖表與互動元件整合預作準備
- 清理 `.gitignore`，補上開發過程中產生但不應追蹤的檔案

### 2026/03/06 Phase 2 通知系統與任務指派完成
- **通知系統：**
  - 後端新增通知相關流程，支援 timeline 邀請後自動建立通知
  - 新增 `GET /api/timelines/upcoming` 與 `GET /api/tasks/upcoming`，提供首頁即將到期/落後項目資料
  - `Header.vue` 加入通知數輪詢與通知面板 UI
  - `HomeView.vue` 整合即將到期與進度落後清單
- **Timeline 任務成員指派：**
  - `TimelineDetailDialog.vue` 任務列表與任務詳情補上成員指派入口
  - `openTaskDetail()` 同步載入 timeline 成員列表，支援快速指派
- `.gitignore` 再次整理，移除不必要追蹤項目

### 2026/03/07 Phase 2 數據分析儀表板與全站 ConfirmDialog 完成
- **數據分析儀表板：**
  - 後端 `profile.py` 新增 `GET /api/profile/chart-stats`
  - 後端 `timelines.py` 新增 `GET /api/timelines/:id/member-stats`（owner 限定）
  - 前端 `profileService.js` / `timelineService.js` 補上圖表資料 API
  - `stores/profile.js` 新增 `chartStats`、`chartLoading`、`ownedTimelines`、`fetchChartStats()`
  - `ProfileView.vue` 整合 ECharts，完成個人層與專案層兩級統計圖表
- **全站 confirm() 替換：**
  - 建立 `composables/useConfirm.js` 與 `components/ConfirmDialog.vue`
  - 全站 6 個檔案、16 處原生 `confirm()` 改為 Promise-based ConfirmDialog

### 2026/03/11~12 Phase 2 完成與 Phase 3 前置整理
- **行動版 RWD 全站適配完成：**
  - `App.vue` 新增 `isMobile` / `sidebarOpen` 控制，手機預設單欄布局
  - 手機底部導航列完成，五個主頁面可直接切換
  - 全部 View 補上底部安全間距，避免內容被底部導航遮住
  - `Header.vue` 手機版收斂 spacing 與通知面板寬度
  - `TimelineViewModes.vue` 補上行動版捲動安全距離
- **CSS 問題修復：**
  - 修正 `style.css` 中全域 `* { padding: 0 }` 未包在 `@layer base`，導致 Tailwind padding utilities 被靜默覆蓋
- **Phase 3 前置整理：**
  - 移除 `TimelinesView.vue` 未使用死碼 `getDaysRemaining`
  - 更新 TypeScript 遷移計畫，明確列出基礎層、Service 層、Store 層與 Composables 的遷移清單
  - 補充 `trashService`、`notifications store`、`DaysRemainingResult` 等遷移注意事項

### 2026/03/13 Phase 3 基礎層 + Service + Store + Composables 完成，並修正 UTC 時區問題
- **TypeScript 遷移（基礎層 + Service + Store + Composables）：**
  - 安裝 TypeScript 5.9.3，新增 `frontend/tsconfig.json`
  - 更新 `frontend/vite.config.js`，加入 `@` alias
  - 新建 `frontend/src/types/index.ts`，定義 User / Todo / Task / Timeline / Group / Notification / Profile 等共用型別
  - `frontend/src/utils/formatters.js` → `formatters.ts`
  - 新建 `frontend/src/utils/validators.ts`
  - `frontend/src/services/api.js` → `api.ts`
  - 完成 `todoService.ts`、`taskService.ts`、`timelineService.ts`、`groupService.ts`、`profileService.ts`、`trashService.ts`、`notificationService.ts`
  - 完成 `stores/auth.ts`、`stores/todos.ts`、`stores/tasks.ts`、`stores/timelines.ts`、`stores/groups.ts`、`stores/profile.ts`、`stores/notifications.ts`
  - 完成 `composables/useConfirm.ts`
  - 刪除已完成遷移的舊 `.js` 檔案
  - 前端 build 驗證通過
- **UTC 時區問題修復：**
  - 修正後端多個 blueprint 與 model 使用 `datetime.utcnow()` 搭配 `.isoformat()` 時未帶時區標記，造成前端顯示提前 8 小時
  - 所有 datetime JSON 輸出統一補上 `Z` 後綴，讓瀏覽器正確以 UTC 解析後再轉本地時間

### 2026/03/15 Phase 3/4 契約收尾
- 完成 Phase 3 元件層 TypeScript 遷移，前端元件改為 `<script setup lang="ts">` 並補齊 Props/Emits 型別
- 完成 Phase 4 子階段：Payload Contract / Guardrails
- 前端新增 `frontend/src/utils/payloadMappers.ts`，統一 create/update payload 組裝
- 後端 tasks/todos/timelines/profile update 路由完成 allowlist 與 unknown field reject
- service mutation 回傳型別與後端契約一致化（`ApiMutationResponse` / `ApiCompletionResponse` / `ApiTaskStatusResponse`）
- 新增 `frontend/scripts/check-payload-guardrails.mjs` 與 `npm run guardrails:payload`
- 驗證結果：`npm run guardrails:payload` 與 `npm run build` 皆通過

### 2026/03/16 Phase 4 甘特圖 + Service Layer 完成
- 完成 `frappe-gantt` 整合（拖曳回寫、hover popup、label 截斷）
- 完成後端 Service Layer 全藍圖接線（task/timeline/todo/profile/group/notification/trash/auth/message）
- 第二輪瘦身：`tasks/timelines` 序列化與名單組裝下沉到 service helper
- 驗證結果：`python -m compileall blueprints services models app.py` 通過

### 2026/03/18 Phase 4.11 WebSocket Slice A/B/C 與分層重整完成
- 後端完成 Socket 事件：join/leave/send-message/broadcast/error（threading 模式）
- 前端完成 Groups Socket 疊加與 message_id 去重，保留 REST fallback
- 完成授權與隔離驗證：非成員拒絕、跨群不串流、雙端同步與聊天室關閉清理
- 結構重整：新增 `backend/realtime/`，搬移 socket 事件檔並更新 `app.py` 匯入
- 啟動流程調整：`start_all.bat` 後端改 `python app.py`，確保 Socket server 正常啟動
- 驗證結果：`python -m compileall blueprints services models realtime app.py` 通過，前端 `npm run build` 通過

### 2026/03/22 Phase 5 部署完整收斂與上線範圍確認
- **階段**：Phase 5.1 ~ 5.4A 全數完成，生產環境正式上線
- 完成 PostgreSQL 遷移（5.1）：Supabase 15 張表初始化與連線驗證通過
- 完成 Railway 後端部署（5.2）：main 自動部署、Gunicorn + Eventlet 配置完成
- 修復 4 波循環導入與生產相容性問題（Werkzeug、Nixpacks、依賴版本）
- 完成 Firebase 前端部署（5.3）與 CORS 放行（含 `https://prajekt-kiriris.web.app`）
- 完成 5.4A 收斂：核心流程驗收、README 與文件同步、AI 任務生成排除本輪上線

### 2026/03/25 前端測試完整套件建立與品質評估
- **階段**：Phase 5.5 前端測試框架就位
- 完成 Vitest 測試：85/85 通過，平均 statements 覆蓋率 88.85%
- Services/Stores/Utils/Composables 測試皆建立完成，TypeScript 0 errors
- 測試缺口明確化：Branch coverage 71.56%，UI 層與 Router guard 仍待補測

### 2026/03/26 後端測試規劃與 Stryker 變異測試安裝完成
- **階段**：Phase 5.6 規劃 + Phase 6.1 啟動
- 完成後端測試規劃：pytest + pytest-flask + SQLAlchemy fixtures，目標覆蓋率 80%+
- 完成 Stryker 安裝與配置，先聚焦 `src/utils/**/*.ts`（353 變異體）
- 修正 Stryker 配置驗證與型別標註問題，確認基礎測試可正常執行
- 待續：完整 mutation run、survived mutants 分析、補測迴圈

### 2026/03/29 後端測試與 CI coverage 報告落地
- **階段**：Phase 5.6 完成 + CI/CD（Backend）落地
- 完成後端藍圖與 service 測試擴充（含 auth/tasks/timelines/todos/groups/profile/notifications/trash/messages）
- GitHub Actions backend workflow 上線：PR/Push 自動執行 `pytest + coverage`
- 覆蓋率報告基線：`80.59%`（`108 passed`）
- 測試產物與快取清理完成（`frontend/coverage`、`stryker-*.txt`、`.pytest_cache`、`.coverage`）

### 2026/04/01 後端全面安全強化與合規修補
- **階段**：CI/CD 輕量版持續補強 + 合規檢查完成
- **API 授權層強化：**
  - 檔案下載端點改為需 JWT 驗證 + task 成員權限檢查（非成員回 403）
  - 完成檔案上傳後回傳欄位一致性修正（`original_filename` 回傳真正原始檔名）
- **請求防呆與例外隱蔽：**
  - 統一新增 JSON payload 物件驗證（`_get_json_dict_or_400` helper）於 6 個藍圖：tasks/timelines/todos/auth/groups/profile
  - 替換所有 500 回應從直接回傳例外內容改為通用通用錯誤訊息，涵蓋 8 個藍圖（tasks/timelines/todos/trash/messages/auth/groups/profile）
  - **修補端點數**：tasks 16 個、timelines 8 個、todos 4 個、auth 2 個、groups 4 個、trash 4 個、messages 1 個、profile 1 個
- **資料合規與驗證：**
  - Profile 端點補上 email 非空字串檢查、trim 正規化、以及重複 email 409 衝突檢查
  - 生產環境密鑰保護：若 `FLASK_ENV` 或 `APP_ENV == 'production'` 且 `SECRET_KEY` / `JWT_SECRET_KEY` 為預設值，啟動時拋錯
- **測試與驗收：**
  - 更新 2 處回歸測試：task 檔案下載新增 401/403 驗證、profile email 唯一性驗證新增
  - 測試結果：**109 passed**（+1，新增 email 重複測試）
- 驗證結果：後端編譯驗證通過，所有測試保持綠燈（109/109）

### 2026/04/01 決策：LM Studio → Gemini（Phase 6.2 技術決策）
- 原計畫：本地 LM Studio + Qwen3.5-9B（無雲端依賴）
- 實際遇到：
  - Qwen3.5 extended thinking 導致 Flask-SocketIO 連接中斷
  - LM Studio 日誌："Client disconnected. Stopping generation..."（20-30 秒後超時）
  - 回應 `content` 為空，思考過程無法提取
- 決策：改用 Gemini 2.5 Flash Lite
  - 優勢：穩定、快速（~3s/請求）、JSON 格式 100% 正確
  - 成本：相對便宜（每月推測 <$5 為少量 API 調用）
  - 限制：需 `GOOGLE_API_KEY` 環境變數（RFC 5252 compliance）
  - 後期復原：只需修改 `generate_task_comment_summary()` 此一函式
- 實裝：
  - 後端完成 `google-generativeai` 集成
  - 移除舊 LM Studio 107 行代碼（`_lmstudio_request_json` + urllib imports）
  - 前端已接線，UI 驗證通過
  - 部署到 Railway 時需配置 `GOOGLE_API_KEY` 密鑰

### 2026/04/02 決策：後端分層收斂完成（auth/groups/tasks）
- 完成 `Blueprint -> Service -> Repository` 收斂，auth、groups、tasks 主線已統一分層。
- `task_service.py` 已移除棄用 LM Studio dead code 與 debug prints，保留既有 API 契約。
- 回歸驗證：`pytest tests -q` 結果 **136 passed**。
- 收斂報告：`docs/後端分層重構收斂報告_2026-04-02.md`

### 2026/04/04 Phase 6.0：本地 PostgreSQL 遷移完成（schema + data）
- 修復 migration 鏈兼容問題：
  - `add_deleted_field_to_timeline_and_task.py` 修正 `down_revision`。
  - `439237f905ed_todo_add_content_field.py` 補 `has_table` 保護與安全刪表順序。
  - `a5efd8d22631_remove_progress_field_from_timeline.py` 補欄位存在檢查。
  - 新增 `b4c2d9e7f1ab_add_subtasks_table.py` 補齊 `subtasks` schema。
- 新增資料遷移工具：`backend/migrate_sqlite_to_postgres.py`（含布林型別轉換、sequence 重置）。
- 完成 SQLite -> PostgreSQL 資料遷移，主要業務表筆數對帳一致。
- 啟動腳本與文件更新：
  - `start_all.bat` 改為 PostgreSQL 主線（自動 `docker compose up` + `flask db upgrade`）。
  - 新增 `bootstrap_pg_local.bat` 一鍵初始化腳本。
  - `README.md` 補齊本地快速部署流程。
- 驗證結果：`/api/health` 顯示 PostgreSQL，後端回歸 `136 passed`。

### 2026/04/05 Phase 6.3：RAG-B 群組快照核心完成（Digest v1）
- 完成 `group_ai_snapshots` model + migration，打通快照落地結構。
- 完成群組快照服務主線：訊息擷取、分段解析、合併、持久化與非同步 job 狀態查詢。
- 完成 API：
  - `POST /api/groups/<id>/ai-snapshot`
  - `GET /api/groups/<id>/ai-snapshot/latest`
  - `GET /api/groups/snapshot-jobs/<job_id>`
- 快照輸出從長摘要收斂為行動導向 Digest（`overview` / `todo_for_user` / `watch_out` / `decisions_brief`）。
- 前端快照視窗同步改為短版可執行摘要，降低閱讀成本。
- 回歸驗證：後端測試 `144 passed`。

### 2026/04/06 Phase 6.3+：Copilot + MCP 整合核心完成（自然語言路由 → 後端工具執行）
- **完成項目：**
  - MCP 伺服器擴展：新增 `timeline_generate_tasks(timeline_id, project_name, description)` 與 `timeline_batch_create_tasks(timeline_id, tasks)` 工具
  - 後端 MCP 橋接層（`backend/services/mcp_bridge_service.py`）：JSON-RPC 子程序通訊、Bearer token 傳播、彈性多行 JSON 解析（處理 startup 輸出噪音）
  - 後端 Copilot 編排層（`backend/services/copilot_service.py`）：AI 工具選擇（Gemini 路由）+ keyword fallback + 參數規範化 + 批量任務自動創建
  - Flask API 端點（`backend/blueprints/copilot.py` @ `/api/copilot/mcp/execute`）：JWT 提取與傳播、執行追蹤回傳
  - 前端 UI 增強（`TimelineDetailDialog.vue`）：自然語言輸入 textarea + MCP routing 複選框 + 自動創建複選框、雙模式任務生成流
  - 前端類型契約與服務層：`frontend/src/types/copilot.ts` + `frontend/src/services/copilotService.ts` + `timelineService` 可選 payload 支持
- **測試與驗證：**
  - 前端單元測試：4 tests passed（endpoint 映射 + optional payload）
  - Python 語法驗證：所有新增/修改後端檔案 compile 無誤
  - MCP 工具發現 smoke test：確認 4 工具註冊（task_comment_summary / timeline_generate_tasks / timeline_batch_create_tasks / group_snapshot）
  - 文件更新：重構計畫、進度追蹤、README 同步完成
- **架構與成果概述：**
  Copilot 可直接調用後端 MCP 工具，無需 Inspector UI；支持自然語言智能路由至最合適的工具；支持快速批量創建任務；保留直接 API 模式作為 fallback

### 2026/04/11 Phase 6.6：LangChain 遷移文檔化完成 + 規劃章節重排收斂
- **完成項目：**
  - Phase 6.6 核心收斂：PromptTemplate 跳脫修復（`{ }` -> `{{ }}`）、舊解析/分支清理、服務層路徑統一
  - 完成遷移文件：`docs/Phase6_6_LangChain_Migration_Guide.md`（架構圖、14 步流程、責任分工、常見問題）
  - 重排 Phase 6 章節順序（6.4 -> 6.5 -> 6.6），移除重複段落，統一狀態描述
  - 校正專案結構區塊（README / 重構計畫）為 2026/04/11 實際目錄（含 `backend/chains`、`backend/prompts`、`mcp_server.py`）
  - 當時已完成根目錄與 `docs/` 雙份主文件同步；目前專案已收斂為僅維護 `docs/` 正式版本
- **測試與驗證：**
  - AI 路徑驗證：LangChain 工作流匯入與調用驗證通過
  - 回歸結果：後端全量測試維持 `153 passed`
  - 文件一致性：當時雙份文件章節與內容對齊；目前以 `docs/` 版為正式文件

### 2026/04/17 Phase 7.1：週報與衝突檢查 MVP 完成 + 安全修補
- **核心功能完成：**
  - **週報 API**（`GET /api/timelines/<id>/weekly-report`）：支援日期範圍篩選、任務完成度追蹤、風險項揭示、留言摘錄、AI 生成週報總結
  - **衝突檢查 API**（`POST /api/timelines/<id>/conflict-check`）：偵測同專案任務重疊、跨專案時間衝突、每日工作量超載，含隱私遮蔽（非所有者隱藏他人時間）
  - **任務指派多選支持**：`assignee_user_ids` 陣列取代舊 `assistant` 單人文字欄位，支援一任務多人指派
  - **隱私遮蔽機制**：衝突檢查回應中非所有者無法看到他人詳細時間衝突（僅顯示「衝突存在」標記）
- **安全強化與修補：**
  - **修補 1**：Conflict check API 補加 assignee 成員資格驗證（line 721）— 未授權指派者於成員校驗階段直接拒絕 400
  - **修補 2**：Conflict check 任務篩選改為排除已完成任務（line 732）— 避免已結案任務產生誤報衝突
  - 新增 2 項回歸測試驗證修補：`test_check_timeline_task_conflicts_ignores_completed_tasks_in_same_timeline` + `test_check_timeline_task_conflicts_rejects_non_timeline_member_assignee`
  - 端點驗證：`backend/tests/blueprints/test_timelines.py` 新增非成員指派拒絕驗證（line 639-649）
- **測試與驗證：**
  - 後端服務層測試：5 項測試通過（包含新增 2 項）
  - API 端點測試：1 項整合測試通過
  - 前端相關測試：6 項通過（timelineService + conflict + assignment 流程）
  - 回歸驗證：全量後端 `153 passed`、前端 `85/85 passed`
  - 代碼品質：timeline_service.py 與 test 檔案零語法錯誤
- **文件同步更新：**
  - 重構計畫（根目錄 + docs/）：Phase 7.1 標記完成、新增 8 項實裝細節
  - 進度追蹤（根目錄 + docs/）：新增 Phase 7.1 里程碑、更新 Phase 7 小節（7.1 ✅、7.2/7.3 ⏳）
  - README.md：新增排程協作與專案週報功能項、新增 weekly-report 與 conflict-check API 端點、更新任務 POST 描述、新增 Phase 7 精簡路線展開圖

### 2026/04/17 後端分層重構收斂：Service/Repository 邊界清理
- **重構範圍：**
  - 權限 decorator 自 service 抽離至 `backend/blueprints/guards.py`，`tasks.py` / `timelines.py` 改由 blueprint 層匯入
  - 新增 repository：`message_repository.py`、`notification_repository.py`、`profile_repository.py`、`todo_repository.py`、`trash_repository.py`
  - 擴充 `timeline_repository.py`、`task_repository.py`、`group_repository.py`，承接 service 原本散落查詢
  - `task_service.py`、`timeline_service.py`、`group_service.py`、`message_service.py`、`notification_service.py`、`profile_service.py`、`todo_service.py`、`trash_service.py` 全面改走 repository
- **測試與驗證：**
  - 後端全量回歸：`162 passed`
  - 錯誤檢查：本輪改動檔案 `No errors found`
  - 掃描 service 直接查詢（`db.session.query` / `.query.` / `db.session.get`）：0 命中
- **文件同步更新：**
  - 已同步更新重構計畫歷史記錄與進度追蹤快速時間軸（根目錄 + `docs/`）

### 2026/04/20 Phase 7.2：進度風險分析核心落地 + 依賴圖視覺化
- **核心功能完成：**
  - 任務依賴欄位落地：`Task.depends_on_task_ids`（模型 + migration + create/update 驗證）
  - 關鍵路徑分析：`critical_path_service.py` 輸出 `summary` / `critical_path` / `risk_items` / `warnings` / `graph`
  - 新增風險 API：`GET /api/timelines/<id>/risk-analysis`、`POST /api/timelines/<id>/risk-analysis/notify`
  - 前端風險面板新增依賴圖按鈕與 SVG 佈局渲染（可切換顯示與重新產生）
  - 任務詳情支援編輯前置依賴，儲存後可即時刷新風險分析
- **AI 任務依賴完善：**
  - AI 生成任務可攜帶 `depends_on_task_ids` 與 `depends_on_task_refs`
  - 批次建立流程可解析 ref -> id，並回傳 `ignored_dependency_refs` 供可觀測性
  - 當 AI 未輸出依賴時，採用順序鏈 fallback，避免整批任務依賴為空
- **驗證結果：**
  - 後端：`test_services.py`、`test_timelines.py` 依賴/風險相關測試通過
  - 前端：`TimelineDetailDialog.phase7.test.ts` 通過；風險圖按鈕與依賴編輯流程可用

### 2026/04/27 Phase 7.3：知識增強後端核心完成 + 文件同步 + push
- **核心功能完成：**
  - 建立知識文件索引主線：`knowledge_documents` / `knowledge_chunks` + pgvector migration + per-user `sha256` 去重
  - 新增知識索引服務：`knowledge_service.py`、`text_splitter_service.py`，支援 md/txt/pdf 上傳、切塊、embedding、重建索引
  - 新增 RAG 規劃服務：`rag_planning_service.py`，整合歷史任務 + 個人知識來源，輸出可追溯 `source_references`
  - Embedding 服務改為 LangChain Provider 架構（預設 Gemini，可切換 openai / huggingface / ollama）
  - 新增 API：`POST /api/timelines/ai-suggest-plan`、`/api/knowledge/documents`（upload/list/delete/reindex）
- **文件與契約同步：**
  - 重構計畫與進度追蹤（根目錄 + `docs/`）同步更新 Phase 7.3 與最新里程碑
  - README 補上知識文件索引、RAG 規劃建議與新 API 清單
- **驗證與推送：**
  - 後端全量回歸 `187 passed`
  - Commit `2ab4894` 已 push 至 `origin/main`

### 2026/04/28 Phase 7.3：安全與契約收斂 + 前端對齊
- **安全修補完成：**
  - 群組成員清單補上成員驗證，非成員直接回 `403`，且預設不回傳 `email`
  - `POST /api/timelines/search_user` 補上 `timeline_id` 範圍限制，僅同專案 owner/member 可查，回傳欄位收斂為 `id` / `name`
- **一致性與效能改善：**
  - 任務列表查詢改為批次抓取 members / users / subtasks，消除 `N+1` 傾向
  - 任務檔案上傳改為 temp 檔 + DB commit 後正式落檔；刪除改為先刪 DB、再 best-effort 清理實體檔
- **前端契約對齊：**
  - `taskService` / `timelineService` / `TimelineDetailDialog` / `TasksView` 同步更新 searchUser 呼叫格式與顯示欄位
- **驗證結果：**
  - 後端聚焦回歸 `93 passed`
  - 前端聚焦回歸 `9 passed`

### 2026/05/06 測試工程化收斂：後端測試檔拆分重構 + 全量回歸
- **拆分完成（降低單檔複雜度）：**
  - `backend/tests/models/test_models.py` 拆分為 `test_user_model.py`、`test_task_model.py`、`test_timeline_model.py`
  - 新增 `backend/tests/models/helpers.py`，統一 model 測試共用 helper
  - `backend/tests/services/test_task_service.py` 抽出序列化測試到 `test_task_service_serializers.py`
  - `backend/tests/services/test_timeline_service.py` 拆分為 `test_timeline_service_ai.py`、`test_timeline_service_reporting.py`、`test_timeline_service_conflicts.py`、`test_timeline_service_access.py`
- **收益：**
  - 測試責任邊界更清晰（AI/衝突/報表/權限分離）
  - 新增場景時定位更快，降低修改互相干擾
  - 便於後續以主題/檔案粒度執行回歸
- **驗證結果：**
  - timeline 相關測試：`18 passed`
  - 後端全量測試：`185 passed`
  - 拆分後無語法錯誤與匯入錯誤

### 2026/05/08 Phase 7.3：個人知識庫前端 + Project Files + RAG 穩定化閉環
- **核心功能完成：**
  - 新增 `/knowledge` 個人知識庫頁與 Sidebar 入口，支援上傳、列表、狀態、搜尋、篩選、排序、刪除與 reindex
  - `TimelineDetailDialog.vue` 補上 Project Files：project-scoped 上傳、篩選、批次刪除、批次重建、下載、預覽與最近操作
  - 知識資料模型補 `project_id`、檔案儲存欄位與 `KnowledgeDocumentEvent`，支援個人與專案知識共用 `knowledge_chunk` 來源契約
  - 修正 migration revision 鏈，避免 Alembic `KeyError` 造成升級中斷
- **RAG 契約與穩定性：**
  - 前後端 `source_references` 統一使用 `timeline_task` / `knowledge_chunk`
  - RAG 規劃納入專案知識檢索，vector 查無結果時改走文字 fallback
  - LLM 生成加入 timeout fallback，避免 `/api/timelines/ai-suggest-plan` 長時間卡住
  - 下載/預覽改為 axios blob + JWT，避免 Vue Router 攔截 `/api/knowledge/.../preview`
- **驗證結果：**
  - 前端聚焦回歸：`timelineService.test.ts`、`KnowledgeBaseView.test.ts`、`TimelineDetailDialog.phase7.test.ts` 共 11 tests passed
  - 後端聚焦回歸：knowledge blueprint / project knowledge / RAG planning 共 8 tests passed
  - 後端全量回歸：`venv\Scripts\python.exe -m pytest`，`189 passed`（僅 `.pytest_cache` 權限 warning）
- 後端語法檢查：`rag_planning_service.py`、`knowledge_repository.py`、`knowledge_service.py` py_compile 通過

### 2026/05/08 工程化收斂：錯誤契約統一 + Blueprint 驗證層收斂 + 前端錯誤處理共用化
- **後端錯誤契約統一：**
  - 新增 `backend/blueprints/validation.py`，集中 `validate_payload_or_400`、`error_response`、`error_from_exception`
  - 錯誤格式統一為 `error` + `error_code` + `error_details`（相容舊前端）
  - 多個 blueprint（auth/tasks/timelines/groups/todos/trash/profile/...）改為同一套 error envelope
- **分層與查詢責任收斂：**
  - 持續下沉 service 中資料庫操作到 repository（task/timeline 主路徑）
  - 保持 `Blueprint -> Service -> Repository` 邊界一致
- **前端錯誤處理共用化：**
  - 新增 `frontend/src/utils/apiError.ts`（`getApiErrorMessage`、`getApiErrorCode`、`mapErrorCodeToMessage`）
  - 多個 View/Store 改為共用解析，移除重複 `axios error.response?.data?.error` 寫法
  - `frontend/src/services/api.ts` 攔截器補 `UNAUTHORIZED` 錯誤碼分支，與 refresh/login flow 對齊
- **文件同步：**
  - 新增 `docs/reference/API_錯誤碼表.md`，提供前端可直接採用的錯誤碼行為對照與建議文案

### 2026/05/16 Phase 8：後端抽象與交易邊界收斂
- **Transaction 邊界收斂：**
  - 新增 `backend/services/transactions.py`，集中 `commit()` / `rollback()` 與 operation error 保留策略
  - `knowledge_service.py`、`task_service.py`、`timeline_service.py` 主線流程改為使用 `with transaction(...)`
  - 移除 task/timeline 舊有 commit helper，避免 service 內散落手動 rollback
- **ORM entity 與 session helper 收斂：**
  - 新增 `backend/repositories/session_repository.py`，統一 `add_entity()` / `flush_session()`
  - `task_repository.py` / `timeline_repository.py` 補上 task、timeline、member、notification 建構 helper
  - `create_task_for_user()`、`batch_create_tasks_for_timeline()`、`create_timeline_for_user()` 改為使用 repository builder + session helper
- **補測與文件：**
  - 補強 task file 測試，確認 DB record 與實體檔案清理一致
  - 新增 `docs/後端Transaction重構順序_2026-05-15.md`
  - 新增 `docs/後端Transaction與ORM抽象說明_2026-05-16.md`
- **驗證結果：**
  - 後端主線回歸：`43 passed`（僅 `.pytest_cache` 權限 warning）
  - Unit of Work / AbstractRepository 暫緩，待讀到 Architecture Patterns with Python 後續章節再評估

### 2026/05/17 Phase 8：收尾強化與一致性修補
- **收尾內容：**
  - `session_repository` 補齊 `delete_entity` 並統一 service 層 session helper 使用方式
  - `group_members` 加入唯一性約束，`join_group_by_invite_code` 補 `IntegrityError -> 409` 防重覆加入
  - `auth_service` / `profile_service` email normalize 對齊（`strip().lower()`）
  - group snapshot jobs 增加 TTL + 最大保留筆數清理策略
  - trash 永久刪除流程語義收斂為「先 DB 後檔案 best-effort 清理」
- **驗證結果：**
  - 聚焦回歸：`55 passed`（`50 + 5`，僅 `.pytest_cache` 權限 warning）

### 2026/05/27 Phase 8.4：前端大型元件拆分收斂
- **重構重點：**
  - `TimelineDetailDialog.vue` 與 `TimelineViewModes.vue` 進一步拆分為多個子元件與編排層
  - 降低單檔責任密度，主元件聚焦 props/emits 與流程編排
  - 保持既有前端行為與 API 契約不變
- **驗證結果：**
  - 前端聚焦測試與 build 維持可用

### 2026/05/27 Phase 8.5：service 複雜度收斂與 Pydantic 契約化
- **後端收斂重點：**
  - 新增後端契約層（現已收斂為 `backend/contracts/`，最初建立 `timeline_contracts.py`、`task_contracts.py`）
  - `timeline_service.py`：`weekly_report` / `conflict_check` / `batch_create_tasks` 主路徑 helper 化與契約化
  - `task_service.py`：`create/update/status` 主路徑契約化，ValidationError 統一映射 OperationError
- **驗證結果：**
  - 後端全量回歸：`194 passed`（僅 `.pytest_cache` 權限 warning）

### 2026/05/27 Phase 8.6：CI 最後一哩與 PR 合併守門落地
- **流程與工作流收斂：**
  - `frontend-tests.yml` 收斂為純 CI（test + build + `guardrails:payload`）
  - 新增 `frontend-deploy.yml`，將部署自測試 workflow 拆分
  - 新增 `.github/pull_request_template.md`，固定風險/測試證據/回滾欄位
  - 新增 `docs/guides/CI_CD_最小流程.md` 與 `docs/guides/Git提交流程規範_給我自己的最小版.md`
- **相容性修補：**
  - 修正 `TimelineDetailDialog.vue` payload 寫法，通過 `guardrails:payload`
- **驗證結果：**
  - `npm run guardrails:payload`：PASS
  - `npm run build`：PASS

### 2026/05/28 Phase 8.7（單人精簡版）：工程護欄與測試工廠化收尾
- **工程護欄收斂：**
  - 8.7 改為單人精簡版，保留高價值流程：PR+CI、smoke test、checkpoint
  - 同步 `docs/guides/CI_CD_最小流程.md`、`docs/guides/Git提交流程規範_給我自己的最小版.md`、8.7 計畫檔
- **後端測試工廠化（A/B/C）：**
  - `backend/tests/conftest.py` 新增 `user_factory`、`timeline_factory`、`task_factory`、`auth_user_factory`
  - `backend/tests/services/` 全批 `_create_user` 收斂至 `user_factory`
  - `backend/tests/blueprints/` 全批 `_create_user` 收斂至 `auth_user_factory`
  - 修正 1 處 timeline blueprint 對固定名稱的硬編碼斷言
- **驗證結果：**
  - `venv\\Scripts\\python.exe -m pytest tests/services`：`79 passed`
  - `venv\\Scripts\\python.exe -m pytest tests/blueprints`：`94 passed`

### 2026/05/31 Phase 9.3：單體 Tool Registry + LangGraph ReAct Agent 首版落地
- **核心完成項目：**
  - 新增 tool 契約層：`tool_inputs.py` / `tool_outputs.py` / `tool_envelopes.py`
  - 新增工具白名單與執行層：`services/tools/registry.py` + `services/tools/handlers.py`
  - 新增錯誤語意映射：`services/tools/error_mapper.py`（`error_code/retryable/hint`）
  - 新增 LangGraph 最小閉環：`agent_state.py` / `agent_nodes.py` / `agent_graph.py`
  - 新增 Copilot Agent API：`POST /api/copilot/agent/execute`、`GET /api/copilot/agent/tools`
  - 前端新增全域入口：`CopilotDock.vue`（右下角浮動入口，context 由系統自動帶入）
- **能力邊界（目前）：**
  - 已支援多步流程：建立專案 -> AI 生成任務 -> 批次建立任務（含依賴鍊）
  - 仍屬第一版白名單，未覆蓋全部 service 對外函式
- **驗證結果：**
  - `venv\\Scripts\\python.exe -m pytest tests/chains/test_agent_graph.py tests/services/test_tool_registry.py -q`：`8 passed`

### 2026/05/31 Phase 9.4：Plan -> Confirm -> Execute 雙階段 Agent 完成
- **核心完成項目：**
  - 新增 `agent_plan_service.py`，管理 plan TTL 與狀態流轉（`planned/executing/succeeded/failed/rejected/expired`）
  - 新增 API：`POST /api/copilot/agent/plan`、`POST /api/copilot/agent/reject`、`POST /api/copilot/agent/replan`
  - `POST /api/copilot/agent/execute` 改為僅執行已確認的 `plan_id`
  - `CopilotDock.vue` 新增前端確認面板，先顯示計畫摘要、步驟預覽、風險提示，再由使用者確認執行
  - execute 階段禁止覆寫已核准 `tool_payloads`，避免 preview 與實際寫入不一致
- **驗證結果：**
  - 補上 plan / execute / reject / re-execute 保護相關服務測試

### 2026/06/02 Phase 9.5：模型提案式 Plan + Replan 強制模型重提案完成
- **核心完成項目：**
  - 新增 `tool_plan_service.py`，由模型依工具清單、描述、schema、side effect 與 context 產生 plan proposal
  - `create_copilot_agent_plan()` 改為 LLM proposal 優先，失敗才 fallback `build_pending_tools`
  - `replan` 改為強制模型重提案，不再默默沿用規則 fallback
  - plan record 與 API 回傳新增 `proposal_source` / `proposal_reason`
  - `CopilotDock.vue` 顯示提案來源與提案說明，讓使用者知道目前是模型提案或規則回退
  - `copilot_service.py`、`tool_plan_service.py` 改為延遲匯入，降低測試收集期對 `langgraph` 的硬耦合
- **驗證結果：**
  - `pytest tests/services/test_copilot_plan_flow.py -q`：`9 passed`
  - `npm run build`：PASS

### 2026/06/05 Phase 9.5 後續補強完成：Agent 契約一致性與安全邊界
- **本輪完成項目：**
  - `plan / replan / execute` 的 `tool_payloads` 契約正式對齊
  - Tool Registry 補上 handler exception boundary
  - `agent_nodes.py` 改為防禦式讀取 state / step
  - planner prompt 補上 protected keys 規則與 steps 上限
  - `TaskCreateInput` mutable default list 改為 `Field(default_factory=list)`
  - `create_notification()` 改為 warning log，避免無聲吞錯
  - `update_task_for_member()` / `update_timeline_for_member()` 新增 `operator_user_id` 並在 service 內驗權限
- **驗證結果：**
  - 後端聚焦回歸：`46 passed`
  - 前端聚焦回歸：`11 passed`
  - `npm run build`：PASS
- **對應文件：**
  - `docs/phases/Phase9_5_Agent契約一致性與安全邊界補強計畫_2026-06-05.md`

### 2026/06/07 README / docs 結構整理完成
- **本輪完成項目：**
  - `README.md` 首頁結構重寫，收斂為較適合對外閱讀的專案入口
  - 正式文件收回 `docs/`：
    - `架構與責任邊界.md` → `docs/architecture/`
    - `API_契約與錯誤處理.md` → `docs/reference/`
    - `本地開發測試部署_Runbook.md` → `docs/runbooks/`
    - `commit-message.md` / `commit_message_zh.md` → `docs/guides/`
  - root 重複主文件移除，後續以 `docs/重構計畫.md`、`docs/進度追蹤.md` 為正式版本
  - Windows 本地啟動腳本移至 `scripts/dev/`，並同步更新 README / runbook 路徑
- **狀態判定：**
  - 本輪屬 repo 入口與 docs 結構收斂，不新增產品功能

### 2026/06/08 backend 結構與前後端契約驗證補強
- **本輪完成項目：**
  - 新增 backend 層級 README：
    - `backend/README.md`
    - `backend/models/README.md`
    - `backend/prompts/README.md`
    - `backend/realtime/README.md`
    - `backend/migrations/README.md`
    - `backend/tests/README.md`
    - `backend/scripts/*/README.md`
  - 將維護型 / 一次性腳本移出 `backend/` 根層，收斂到 `backend/scripts/backfill/`、`backend/scripts/db/`、`backend/scripts/diagnostics/`
  - `POST /timelines/{id}/conflict-check` payload 契約補上 `task_id`，對齊前端 `ConflictCheckPayload`
  - `POST /tasks/{id}/members`、`POST /timelines/{id}/members` 成功回傳改為真正回傳新增後的 member 資料
  - 前端 `TaskPriority`、`CreateTaskPayload.end_date`、`ConflictCheckPayload.start_date/end_date` 等型別收斂
  - `payloadMappers` 補強缺欄位中止與 priority 正規化
  - 補齊 task / timeline 高風險 blueprint 契約測試（缺欄位、錯型別、成功 body）
- **驗證結果：**
  - `pytest backend/tests/blueprints/test_tasks.py backend/tests/blueprints/test_timelines.py -q`：`38 passed`
  - `python -m py_compile backend/blueprints/tasks.py backend/blueprints/timelines.py backend/services/task_service.py backend/services/timeline_service.py`：PASS
  - `npm run build`：PASS

### 2026/06/09 Phase 9.5：Agent 契約與邊界大修完成（9.6 前置基線整理）
- **本輪完成項目：**
  - `tool_inputs.py` 內層契約硬化：`CreateTaskToolPayload`、`UpdateTaskToolPayload`、`CreateTimelineToolPayload`、`BatchCreateTaskItem`、`ConflictCheckInput` 對齊
  - `TimelineGenerateTasksToolInput` 補 `actor_user_id`，讓工具路徑的專案讀取權限可落在 service 驗證
  - `agent_nodes.py` 與 `copilot_service.py` 改為遞迴 sanitize nested payload，統一清除 protected keys
  - 高副作用工具的 retryable error 不再自動重試，改走 `ask_user`，避免部分寫入後重覆執行
  - `task_service.py`、`timeline_service.py`、`knowledge_service.py` 補 service 內 actor / member guard，降低只靠 blueprint guard 的風險
  - `batch_create_tasks_for_timeline` 語意改收斂為「套用任務規劃」，同步修正 Agent 成功摘要與 CopilotDock 執行結果文案
- **驗證結果：**
  - focused pytest：`74 passed`
  - 指令：`.\venv\Scripts\python.exe -m pytest tests/services/test_tool_registry.py tests/chains/test_agent_graph.py tests/services/test_timeline_service_ai.py tests/services/test_task_service.py tests/services/test_project_knowledge.py tests/services/test_copilot_plan_flow.py`
- **狀態判定：**
  - 本波可視為完成
  - 完整 idempotency key 設計保留為後續 follow-up，不阻塞進入 9.6

### 2026/06/10 第三波目錄清理延伸：TimelineDetailDialog modal/panel 再拆分
- **本輪完成項目：**
  - `TimelineDetailDialog.vue` 再拆出三個低風險子元件：
    - `TimelineAddTaskModal.vue`
    - `TimelineSharePanel.vue`
    - `TimelineTaskMemberPanel.vue`
  - 再拆出兩個高資訊密度但流程相對獨立的面板：
    - `TimelineWeeklyReportPanel.vue`
    - `TimelineRiskAnalysisPanel.vue`
  - 父元件保留 service 呼叫、權限、toast、confirm 與編排責任，子元件只承接表單 / 呈現 / emit
  - 前端入口殘留同步收斂：`main.ts`、`router/index.ts` 已成為正式結構描述
- **量化結果：**
  - `TimelineDetailDialog.vue`：`2152 -> 1637` 行（累計降 515 行）
- **驗證結果：**
  - `npm run build`：PASS
  - `npm run test -- TimelineDetailDialog.phase7.test.ts TimelineSubcomponents.test.ts TimelineViewModes.test.ts`：`27 passed`

### 2026/06/11 Phase 10 前置收斂：tool planner prompt 抽離與 repo 結構規範補齊
- **本輪完成項目：**
  - 將 `backend/services/tool_plan_service.py` 內嵌 planner prompt 抽離至 `backend/prompts/tool_planner.py`
  - 新增 `TOOL_PLANNER_SYSTEM_PROMPT` 與 `build_tool_planner_prompt(...)`，讓固定規則留在 `prompts/`、動態資料由 builder 組裝
  - `backend/prompts/__init__.py` 同步匯出 `tool_planner` 入口，對齊既有 prompt 集中管理方式
  - 補強 `backend/prompts/README.md`，明確寫出 `PromptTemplate` 與 builder helper 的使用分界
  - 補齊多個 repo README / 規範文件，特別是：
    - `frontend/src/README.md`
    - `frontend/src/components/README.md`
    - `frontend/src/components/timelines/README.md`
    - `frontend/src/components/__tests__/README.md`
    - `frontend/src/composables/README.md`
    - `frontend/src/router/README.md`
    - `frontend/src/services/README.md`
    - `frontend/src/stores/README.md`
    - `frontend/src/styles/README.md`
    - `frontend/src/types/README.md`
    - `frontend/src/utils/README.md`
    - `frontend/src/views/README.md`
    - `scripts/README.md`
  - 補強 `backend/contracts/README.md`、`backend/services/tools/README.md`，收斂 agent `contracts -> tools -> services` 的責任邊界
- **本輪收斂重點：**
  - 不新增產品功能，優先補清楚 prompt / contract / tool / type / utils / service / test 的目錄規範
  - 讓 Phase 10 的「重新釐清契約並熟悉專案」有明確落點，而不是只停在抽象規劃
- **驗證結果：**
  - `python -m py_compile backend\\prompts\\tool_planner.py backend\\prompts\\__init__.py backend\\services\\tool_plan_service.py`：PASS

### 2026/06/12 Phase 10.1：全系統契約地圖盤點與第一批對齊完成
- **本輪完成項目：**
  - 完成 `auth/profile`、`tasks`、`timelines`、`knowledge`、`groups/messages/notifications`、`todos/trash`、`copilot/agent` 的後端契約來源盤點
  - 補出 Phase 10 第一版契約地圖，讓 blueprint payload、service contract、tool contract、response shape 的真相來源可被追蹤
  - 同步完成第一批前後端契約對齊，涵蓋 task、group、notification、trash、todo、timeline、copilot 等主線模組
- **本輪收斂重點：**
  - Phase 10 不再只停留在抽象「要收斂契約」，而是先把系統中每一類契約的來源、責任與落點盤清楚
  - 為後續 10.2 request contract 收斂與 10.3 response contract 收斂先建立可執行的清單與基線

### 2026/06/12 Phase 10.2 規劃完成：後端契約來源正式切層
- **本輪完成項目：**
  - 新增 `docs/phases/Phase10_2_後端契約來源收斂規劃_2026-06-12.md`
  - 新增 `docs/reference/backend_契約來源索引.md`
  - 明確切出 blueprint payload、validation helper、service contracts、tool contracts、response shape 的收斂方向與後續檢查點
- **本輪收斂重點：**
  - 把「哪裡該驗證輸入」、「哪裡該定義資料契約」、「哪裡只是 route-owned identifier 或 protected field」先講清楚
  - 避免後續實作時一邊搬 schema、一邊重新猜每層責任

### 2026/06/15 Phase 10.2 第一輪實作收斂完成：request contract 主線落地
- **本輪完成項目：**
  - 新增 `shared_fields.py`，收斂可重用欄位與共用 schema 元件
  - 擴充 `auth/profile/knowledge/todo/group contracts`，並延伸收斂 `task_contracts.py`、`timeline_contracts.py`、`tool_inputs.py`
  - `auth/profile/tasks/timelines/knowledge/todos/groups` blueprint 與部分 service 主路徑接上集中 contract
  - 契約來源與命名規範同步補進 README、Phase 文件與索引文件
- **驗證結果：**
  - 聚焦 pytest：`83 passed`
  - 擴大聚焦 pytest：`137 passed`
  - `py_compile`：PASS

### 2026/06/16 Phase 10.2 契約層目錄正式定版：`backend/contracts/` 成為共用真相來源
- **本輪完成項目：**
  - 將 `backend/services/contracts/` 正式搬遷為 `backend/contracts/`
  - backend import、README、契約來源索引與 Phase 10 文件全面對齊新位置
  - 補出 `Phase10_2_後續確認清單_2026-06-16.md` 與 validation helper 位置評估文件，明確記錄當前結論與後續可再調整點
- **本輪收斂重點：**
  - 後續以 `backend/contracts/` 作為 blueprint / service / tool 共用契約的正式真相來源
  - `validation.py` 暫留 `blueprints/`，但已留下未來若結構再擴大時可搬遷的決策紀錄

### 2026/06/17 Phase 10.3：後端 response contract、OpenAPI 與 frontend consumer 主線收尾完成
- **本輪完成項目：**
  - 新增 `backend/contracts/response_contracts.py` 與 `backend/contracts/response_helpers.py`
  - 固定 mutation / counter / auth / tool-list response 改由 Pydantic model 輸出，主要 service payload 逐步改為 schema 驗證後輸出
  - `tasks / timelines / knowledge / groups / profile / todos / trash / notifications / messages` 穩定 response 主線已完成 schema 輸出化
  - `timeline_contracts.py` 補齊 `weekly report`、`risk analysis`、`risk notify`、`conflict-check`、`AI generate`、`AI suggest plan` 等 response models
  - `rag_planning_service.py`、auth current user、login nested user 等既有回傳主線補上正式 domain response schema
  - 建立 `/api/openapi.json`、`/api/docs` 與 `docs/reference/openapi.json` 靜態匯出流程，OpenAPI 第一版已補 requestBody / response schema / components / security scheme
  - frontend 同步完成 auth / upcoming / groups snapshot / profile stats 第一輪 consumer 對齊，並補做 copilot/tool-list 外層型別、殘留 cast / shape guard cleanup、共用 JSON 型別與 api refresh typing 收束
  - 補齊 `protected fields / frontend 不可自送欄位` 正式說明文件，將欄位擁有權邊界文件化
- **驗證結果：**
  - 後端 focused pytest：`125 passed`
  - 前端 focused tests：`57 passed`
  - frontend consumer focused tests：`40 passed`
  - copilot / cast cleanup focused tests：`35 passed`
  - shape-guard cleanup focused tests：`26 passed`
  - final typing cleanup focused tests：`40 passed`
  - frontend build：PASS
  - OpenAPI focused pytest：`6 passed`

---
