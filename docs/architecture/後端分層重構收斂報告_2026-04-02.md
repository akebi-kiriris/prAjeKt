# 後端分層重構收斂報告（2026-04-02）

## 1. 目標與範圍

本次收斂目標：完成 Flask 後端在任務主線上的分層一致化，將 Blueprint 精簡為路由與 HTTP 轉譯層，Service 專注商業規則，Repository 統一資料存取。

收斂範圍：
- auth
- groups
- tasks（含成員、留言、子任務、檔案、AI 摘要協作流程）

## 2. 本次完成事項

### 2.1 分層收斂（Blueprint -> Service -> Repository）

- auth：
  - auth blueprint 已透過 auth service 處理註冊、登入、目前使用者查詢。
  - 新增 auth repository，統一 user 查詢入口。

- groups：
  - groups blueprint 已委派到 group service。
  - group service 已改走 group repository（邀請碼、成員、訊息、群組列表）。

- tasks：
  - tasks blueprint 已維持薄控制器角色。
  - task service 內高頻 direct ORM 查詢已改為 task repository helper。
  - 涵蓋查詢面：任務成員、留言、子任務、檔案、timeline 成員補查等。

### 2.2 代碼清理（第 1 步）

- 移除 task service 已棄用 LM Studio dead code：
  - `_LMStudioRequestError`
  - `_lmstudio_request_json(...)`
  - `_pick_chat_model(...)`
- 移除對 `requests` 的未使用依賴引入。
- 移除 AI 摘要流程中的除錯輸出（print），保留錯誤語意與回應契約不變。

## 3. 驗證結果

- 目標測試（先前分層重構階段）：76 passed
- 本次收斂後全量後端回歸：136 passed
- 測試指令：
  - `c:/Users/USER/Desktop/0611/0611/prajekt/backend/venv/Scripts/python.exe -m pytest tests -q`

## 4. 分層規範（當前準則）

- Blueprint：
  - 只做 input parsing、auth decorator、HTTP status mapping。
  - 不直接承載商業判斷與跨模型流程。

- Service：
  - 聚焦業務規則、權限判斷、跨資源協調與交易邊界。
  - 例外透過 OperationError 類型映射為 API 錯誤。

- Repository：
  - 集中 model query/filter/order/join。
  - 提供可重用 helper，避免 Service 散落直接 ORM 查詢。

## 5. 後續開發守則

- 新增 API 時，優先先設計 service 函式，再補 repository helper，不把 query 寫回 blueprint。
- 若 service 出現重複查詢片段，立即下沉到 repository。
- 每輪重構完成後，至少跑一次全量 `pytest tests -q` 再合併。

## 6. 風險與邊界

- AI 摘要目前以 Gemini 為主，切換模型時應只更動摘要實作函式，避免擴散到 blueprint 與 repository。
- task service 雖已顯著收斂，但仍建議持續保持「單一入口資料存取」原則，避免後續回滲 direct query。
