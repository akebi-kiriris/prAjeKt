# PrAjeKt 功能優先級規劃

> 最後更新：2026/01/21  
> 策略：**精簡開發** - 只做核心，避免 side project 肥大化  
> 原則：能用 > 完美，學習 > 堆功能

---

## 🎯 核心功能（必做）

> 這些是專案的核心價值，沒有這些等於沒有產品

### ✅ 已完成
| 功能 | 檔案位置 | 狀態 |
|------|----------|------|
| 使用者認證（註冊/登入/登出） | `auth.py`, `LoginView.vue`, `RegisterView.vue` | ✅ 完成 |
| 專案 CRUD | `timelines.py`, `TimelinesView.vue` | ✅ 完成 |
| 任務 CRUD | `tasks.py`, `TimelinesView.vue` | ✅ 完成 |
| 軟刪除（Soft Delete） | `timeline.py`, `task.py`, `timelines.py` | ✅ 2026/01/22 完成 |
| 看板模式（三欄拖拽） | `TimelinesView.vue` + vuedraggable | ✅ 完成 |
| 月曆視圖 | `TimelinesView.vue` + FullCalendar | ✅ 完成 |
| 任務優先級/標籤 | `tasks.py`, `TimelinesView.vue` | ✅ 完成 |
| 篩選與搜尋 | `TimelinesView.vue` | ✅ 完成 |
| 個人 Todo | `todos.py`, `TodosView.vue` | ✅ 完成 |
| 個人資料頁 | `profile.py`, `ProfileView.vue` | ✅ 完成 |
| 群組聊天 | `groups.py`, `messages.py`, `GroupsView.vue` | ✅ 完成 |
---

## 2026/01/22 進度補充
- 完成「AI 任務生成」功能，整合 Google Generative AI (Gemini 2.5 Flash Lite)，根據專案名稱/描述自動生成任務清單。
- 完成「軟刪除」(Soft Delete) 機制，專案（timelines）與任務（tasks）皆新增 deleted 欄位，所有刪除操作改為軟刪除，API 查詢自動過濾 deleted=False。
- Alembic migration 衝突解決，資料庫結構升級成功。
- .gitignore 檢查，.env 及所有 .md（除 README）已忽略。

### 原理/流程說明
- AI 任務生成：使用 Gemini API，透過 JSON Schema 限定輸出格式，Prompt 包含專案資訊與現有任務，讓 AI 生成互補任務。
- 軟刪除：資料不直接移除，僅標記 deleted=True，方便還原與保留歷史。
- 查詢時一律過濾 deleted=False，前端只顯示未刪除資料。
- Alembic migration：資料庫結構控管，遇多 head 須合併。

### 🔨 待完成
| 功能 | 說明 | 為什麼是核心 |
|------|------|-------------|
| 🤖 **AI 任務生成** | 創建專案時，AI 根據專案名稱/描述自動建議任務 | **亮點功能**，展示 AI 整合能力 |
| 👥 **基本協作邀請** | 邀請成員加入專案（簡化版，不需複雜權限） | 專案管理基本需求 |
| 📋 **任務指派** | 任務可指派給專案成員 | 協作核心功能 |

---

## 🟡 次要功能（可做可不做）

> 有這些會更好，但沒有也能用

| 功能 | 說明 | 開發成本 | 決定 |
|------|------|----------|------|
| 子任務系統 | 任務下再拆分小任務 | 中（已有 model） | ⏸️ 暫緩 |
| 任務留言區 | 任務內討論 | 中（已有 task_comment） | ⏸️ 暫緩 |
| 檔案上傳 | 留言/任務附加檔案 | 高 | ⏸️ 暫緩 |
| 垃圾桶機制 | 軟刪除、還原 | 中 | ⏸️ 暫緩 |
| 權限分級 | Owner/Editor/Viewer | 中 | ⏸️ 暫緩 |
| 行動版優化 | 底部導航列 | 中 | ⏸️ 暫緩 |

---

## ❌ 不做（優先級低）

> 對 side project 價值不高，時間投入不划算

| 功能 | 原因 |
|------|------|
| 深色模式 | 純累贅，UI 工程 |
| 甘特圖 | 開發成本高，整合第三方庫複雜 |
| 數據分析儀表板 | 需要大量數據才有意義 |
| 通知系統 | WebSocket/推播複雜度高 |
| API 重構 | 技術債，但不影響功能 |

---

## 📍 現有功能地圖

### 後端 API（`backend/blueprints/`）
```
auth.py        → 認證（登入/註冊/登出）
profile.py     → 個人資料
timelines.py   → 專案管理
tasks.py       → 任務管理 + 子任務 + 留言
todos.py       → 個人待辦
groups.py      → 群組管理
messages.py    → 群組訊息
```

### 前端頁面（`frontend/src/views/`）
```
LoginView.vue      → 登入
RegisterView.vue   → 註冊
HomeView.vue       → 首頁/儀表板
TimelinesView.vue  → 專案管理（含看板/月曆）
TasksView.vue      → 任務頁（如有獨立的話）
TodosView.vue      → 個人待辦
GroupsView.vue     → 群組聊天
ProfileView.vue    → 個人資料
```

### 資料模型（`backend/models/`）
```
user.py          → 使用者
timeline.py      → 專案
timeline_user.py → 專案成員關聯
task.py          → 任務
task_user.py     → 任務成員關聯
task_comment.py  → 任務留言
subtask.py       → 子任務（已建，但可暫緩前端）
todo.py          → 個人待辦
group.py         → 群組
message.py       → 群組訊息
notification.py  → 通知（已建，但未啟用）
activity_log.py  → 活動日誌（已建，但未啟用）
```

---

## 🚀 下一步行動

### 立即要做（核心）
1. **AI 任務生成** - 這是最大亮點
   - [ ] 設計 prompt（輸入專案名稱/描述 → 輸出任務清單）
   - [ ] 後端 API：`POST /timelines/:id/generate-tasks`
   - [ ] 前端：創建專案時的「AI 生成」按鈕
   - [ ] 整合 OpenAI/Claude API

2. **基本協作** - 讓專案能多人用
   - [ ] 邀請成員（輸入 email 或 username）
   - [ ] 成員列表顯示
   - [ ] 任務指派下拉選單

### 之後再說
- 上面「次要功能」的項目
- 有時間再回頭優化

---

## 💡 提醒自己

> **這是 side project，不是正式產品**
> 
> - 80% 完成度就夠了
> - 不要追求完美，追求「能 demo」
> - 留時間看代碼、學習
> - 面試時能講清楚原理比功能多更重要
