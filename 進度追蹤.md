# PrAjeKt 進度追蹤

> 最後更新：2026/04/01（Phase 6 主線改版：LM Studio + Qwen3.5-9B + 三個 RAG 功能）
> 開發方式：Vibe Coding（高強度 AI 輔助）
> **當前 Phase：CI/CD 輕量版持續補強 + Phase 6 AI 本地產品主線（規劃中）**

---

## ✅ 已完成

### Phase 3 / Phase 4 收尾（2026/03/15）

- [x] 完成前後端 payload decoupling（Task / Todo / Timeline / Profile）
- [x] 後端 create/update 端點白名單驗證與 unknown field reject
- [x] 前端 service mutation 回傳型別全面對齊後端契約（message / id / task_id / completed / status）
- [x] 新增 mapper 層：`src/utils/payloadMappers.ts`
- [x] `tasks/todos/timelines` 的 create/update payload 改由 mapper 輸出
- [x] 新增 guardrail 腳本：`npm run guardrails:payload`
- [x] 新增 payload 契約文件：`docs/payload-contracts.md`
- [x] `npm run guardrails:payload` 驗證通過
- [x] `npm run build` 驗證通過

### Phase 4：Gantt + 後端 Service Layer（2026/03/16）

- [x] Gantt 改採 `frappe-gantt`，完成拖曳回寫、hover popup、label 截斷
- [x] 修正 `custom_class` 空白 token 導致 `DOMTokenList.add` crash（甘特圖空白）
- [x] 新增任務主責設定 API：`PATCH /tasks/<task_id>/members/<member_id>`
- [x] 主責設定權限修正：任務主責 / 任務建立者 / 專案主責皆可操作，並可自動補建 `task_user`
- [x] 月曆日期改為 local date-only 解析，避免時區偏移錯日
- [x] 後端 Service Layer 完成：`task/timeline/todo/profile/group/notification/trash/auth/message`
- [x] `todos/profile/groups/notifications/trash/auth/messages` blueprints 接線完成
- [x] 第二輪瘦身：`tasks/timelines` 的序列化與名單組裝下沉到 service helper
- [x] 後端編譯檢查：`python -m compileall blueprints services models app.py` 通過

### Phase 4.11：WebSocket 即時通訊落地（2026/03/18）

- [x] 完成前後端盤點（groups/messages/models/store/view/api）
- [x] 確認首版策略：threading + REST fallback（不破壞既有聊天流程）
- [x] 新增實施文件：`docs/WebSocket_群組即時聊天計畫.md`
- [x] Slice A 實作：後端 Socket 事件（join/leave/send/broadcast）
- [x] Slice B 實作：前端 Groups 頁面 Socket 疊加
- [x] Slice C 驗證：授權隔離、回退流程與雙端即時測試

### Phase 5 部署規劃：展示級上線（2026/03/21 決策）

- [x] 評估方案 A（免費）、B（$5-10/月）、C（Cloud Run）
- [x] 選擇方案 B：Railway + Supabase + Firebase（展示級最佳選擇）
- [x] 成本確認：$0 前端 + $5-10 後端 / 月 + $0 資料庫（Supabase 免費版）= **月費 $5-10**
- [x] 與用戶確認部署清單與優先順序
- [x] **Phase 5.1**：資料庫遷移（SQLite → Supabase PostgreSQL）✅ 完成
- [x] **Phase 5.2**：後端部署（Flask → Railway WebSocket）✅ 完成
  - ✅ 創建 Procfile、start.sh、railway.json
  - ✅ 修復 Werkzeug/Gunicorn 生產環境設定
  - ✅ 解決循環導入問題（blueprints/services → models）
  - ✅ Railway 後端成功啟動（http://railway-app:8080）
  - ✅ 資料庫初始化成功、健康檢查正常
- [x] **Phase 5.3**：前端連接調整 + Firebase 部署 ✅ 完成（2026/03/22 晚上）
  - ✅ 環境變量更新（`.env.production` 指向 Railway）
  - ✅ Firebase 專案初始化（`prajekt-kiriris`）
  - ✅ 構建部署成功（npm run build → 17 files → Firebase Hosting）
  - ✅ CORS 配置（backend/app.py 允許 `https://prajekt-kiriris.web.app`）
  - ✅ 傳部署完成（`https://prajekt-kiriris.web.app`）
  - ✅ 三服務聯動驗證通過
- ✅ **Phase 5.4A**：端對端基本驗證（已收斂）
  - [x] 單人核心流程完成確認（註冊 / 登入 / 任務 CRUD / 群組聊天）
  - [x] GitHub README 更新
  - [x] 驗收範圍註記：本輪上線不包含 AI 任務生成
  - [x] 本輪測試待辦移出（改由 Phase 5.6 / CI/CD 追蹤）
- [x] **Phase 5.5**：前端測試框架（Vitest）✅ 完成（85/85 tests，平均 coverage 88.85%）
- [x] **Phase 5.6**：後端測試 + CI coverage 報告 ✅ 完成（`108 passed`，coverage 80.59%）
- ✅ **Phase 5.7**：後端安全強化與合規修補 ✅ 完成（2026/04/01，109 tests passed）
  - ✅ API 授權層強化（檔案下載改需 JWT + task 成員檢查）
  - ✅ 請求防呆（統一 JSON payload 驗證）與例外隱蔽（44 個端點改用通用訊息）
  - ✅ 資料合規（email 唯一性驗證、生產密鑰保護）
  - ✅ 測試更新與驗收（109 passed）
- [ ] **Phase 6**：AI 本地主線（Dify + RAG → Agents → MCP）

### Phase 0 — 結構重構

#### Step 0-1：Service 層 ✅ 完成（2026/02/23~24，預估 3 天 → 實際 1 天）
- [x] `services/todoService.js`
- [x] `services/taskService.js`（含 subtask、updateStatus）
- [x] `services/groupService.js`
- [x] `services/profileService.js`
- [x] `services/timelineService.js`
- [x] 所有 View 移除直接 `api.` 呼叫，改用 service 層
- [x] 測試功能正常

#### Step 0-2：Store 層 ✅ 完成（2026/02/25，預估 3~5 天 → 實際 1 天）
- [x] `stores/todos.js`：CRUD + completedTodos/pendingTodos computed + toggleTodo 樂觀更新
- [x] `stores/tasks.js`：CRUD + subtask 操作 + loading 狀態管理
- [x] `TodosView.vue` 改用 useTodoStore
- [x] `TasksView.vue` 改用 useTaskStore
- [x] 測試功能正常

#### Step 0-3：TimelinesView 第一層拆分 ✅ 完成（2026/02/25，預估 1~2 天 → 實際 1 天）
- [x] 建立 `components/timelines/` 資料夾
- [x] 抽取 `TimelineHeader.vue`（125 行）— Header + Stats + View Toggle
- [x] 抽取 `TimelineViewModes.vue`（741 行）— 四種視圖模式 + 看板任務 Modal
- [x] 抽取 `TimelineDetailDialog.vue`（405 行）— 專案詳情 + 任務所有 Modal
- [x] `TimelinesView.vue` 2569 行 → **301 行**（降幅 88%）
- [x] Vite 編譯通過：136 模組，0 錯誤

### 其他修正（夾帶完成）
- [x] `Sidebar.vue` 加 wrapper `<div>` 解決 fragment 根節點警告
- [x] `Sidebar.vue` 加 `defineEmits(['close'])` 解決 non-emits 警告
- [x] `TasksView.vue` 日期格式修正（`T00:00:00` → `.slice(0, 10)`）
- [x] `TasksView.vue` 表單改為 Modal 卡片（Teleport + showForm）
- [x] `tasks.py` assistant 欄位 array → string 轉換修正
- [x] `timelines.py` task_comments SQL 欄位名稱修正（`message` → `task_message`、`id` → `comment_id`）
- [x] `TimelinesView.vue` 硬編碼 URL 改為 `apiBaseUrl` 變數

---

### Phase 1 — MVP 核心功能（已完成 ✅）

#### 多人協作與權限系統 🔴 — 後端完成，前端部分完成（2026/02/26）

**後端（timelines.py）✅ 全部完成：**
- [x] `get_user_timeline_role()` helper（ORM）
- [x] `require_timeline_role('owner'|'member')` decorator factory
- [x] `create_timeline` 建立後自動寫入 `timeline_users`（role=0 負責人）
- [x] `get_timelines` 改 JOIN `timeline_users`，每筆 timeline 含 `role` 欄位
- [x] 所有 timeline 路由套上正確 decorator（owner/member 分級）
- [x] 新增 `DELETE /<id>/members/<member_user_id>` endpoint
- [x] 全面 ORM 化，移除所有 raw SQL
- [x] 移除 6 處無用的 `user_id = int(get_jwt_identity())` 宣告
- [x] 語法驗證通過（`python -c "import ast..."` exit code 0）

**前端（Vue）🔄 部分完成：**
- [x] `timelineService.js` 新增 `removeMember`
- [x] `TimelineViewModes.vue` — ✏️ 🗑️ 按鈕加 `v-if="timeline.role === 0"`（List + Card 共 4 處）
- [x] `TimelineDetailDialog.vue` — 「成員管理」按鈕加 `v-if="selectedTimeline?.role === 0"`

**⚠️ 明天待做（前端剩餘）：**
- [x] 舊資料補填：執行 `backfill_timeline_users.py`（補 5 筆，1 筆跳過）
- [x] 成員管理 Panel UI — 顯示現有成員列表 + 個別踢人按鈕（`kickMember`）
- [x] `timelineService.js` 新增 `getMembers`

**⚠️ 明天待做（tasks 多人協作）：**  
> 詳細缺陷分析已在 2026/02/26 完成，整理如下：
- [x] `tasks.py` 加 `require_task_role()` decorator（參照 timelines 做法，含 timeline 角色繼承）
- [x] `GET /members`、`/subtasks`、`/comments` 加成員驗證（共 14 個路由)
- [x] Subtask CRUD + toggle 加成員驗證
- [x] 統一用 `task_user.role`（+ timeline_user 繼承）做權限判斷
- [x] `delete_task`、管理成員路由改為 owner-only
- [x] 新增 `GET /timelines/<id>/members` 端點（成員面板用）
- [x] **前端任務成員管理 UI**（取代舊 `assistant` 欄位文字輸入）

---

## Phase 5 工作清單（展示級部署）

### 5.1 — PostgreSQL 遷移 ✅ 完成（2026/03/22）
- [x] Supabase 免費專案建立
- [x] 取得連線字串（postgresql://user:pass@db.host/database）
- [x] 後端改用 PostgreSQL driver（psycopg2-binary）— `requirements.txt` 已更新
- [x] 本地 Flask migration 到 Supabase — 直接使用 `init_db.py`（15 個表格建立成功）
- [x] 本地 SQLite 資料導出腳本（展示級，從新建帳號開始）
- [x] 本地連線驗證 — PostgreSQL 連接成功，所有表格已建立
- [x] 後端服務啟動 — Flask app 運行正常，環境變量已配置
- [x] 流程文檔編寫 — [Phase5_1_PostgreSQL遷移流程.md](docs/Phase5_1_PostgreSQL遷移流程.md)

### 5.2 — Railway 後端部署 ✅ 完成（2026/03/22）
- [x] Railway 帳號及專案建立
- [x] GitHub 倉庫綁定 + main 分支自動部署
- [x] 環境變數設定（DATABASE_URL、SECRET_KEY、JWT_SECRET_KEY、FLASK_ENV）
- [x] Flask 啟動改為支援 `PORT` 環變（Railway 動態分配端口）
- [x] 流程文檔編寫：[Phase5_2_Railway後端部署.md](docs/Phase5_2_Railway後端部署.md)
- [x] 初次部署執行 — Railway 自動構建並部署成功
- [x] 資料庫初始化成功 — 15 個表格建立完成
- [x] Gunicorn + Eventlet 生產伺服器配置
- [x] 循環導入問題修復（4 波 commit：2314114, 4b2aa2c, 45eb790, af7b938）
- [x] 取得公網 URL（Railway 動態分配）
- [x] 健康檢查驗證 — `GET /api/health → 200 OK` 成功
- [x] 日誌檢查與 WebSocket 連線驗證

### 5.3 — 前端配置 + Firebase 部署 ✅ 完成（2026/03/22）
- [x] 取得 Railway 公網 URL（prajekt-production.up.railway.app）
- [x] 更新 `frontend/.env.production` 指向 Railway 後端
- [x] 本地 `npm run build` 驗證 — 編譯成功
- [x] Firebase 前端編譯優化 — 預檢查環境、依賴、配置
- [x] Firebase Hosting 部署 — `firebase deploy` 成功
- [x] 前端 URL: https://prajekt-kiriris.web.app ✅
- [x] 驗證 CORS 配置 — 後端允許 Firebase domain
- [x] Commit 0b0d6e0: 添加 Firebase Hosting 域名到 CORS 允許列表
- [x] 流程文檔編寫：[Phase5_3_Firebase前端部署.md](docs/Phase5_3_Firebase前端部署.md)

### 5.4 — 驗證與上線 ✅ 收斂完成（本輪）

#### 5.4A：本輪上線範圍收斂（已完成）
- [x] 單人核心流程完成確認（註冊 → 登入 → 建立任務 → 群組聊天 → 任務操作）
- [x] GitHub README 更新生產 URL與驗收範圍
- [x] 本輪上線驗收範圍明確排除 AI 任務生成

---

## 🔄 進行中 / 待做

> Phase 0～4 功能主線已完成（PostgreSQL 另併入 Phase 5），目前主線為 Phase 6 本地驗證（Dify + LM Studio + Qwen3.5-9B + 三個 RAG + MCP）；本地部署，不進行雲端擴展。

### Phase 1 — 完成清單（全部完成 ✅）

#### 多人協作收尾 ✅ 完成（2026/02/27，超前進度）
- [x] 舊 timelines 補填 `timeline_users` 資料（backfill 腳本）
- [x] 成員管理面板顯示成員列表 + 踢人（前端 UI）
- [x] `tasks.py` 安全漏洞修補 + task_user 多人協作
- [x] 前端任務成員管理 UI（TasksView 成員管理 Panel）

#### 任務留言與檔案上傳 ✅ 完成（2026/03/01~02）
- [x] 後端 `timelines.py` 新增任務留言 CRUD（get/add/delete）
- [x] 後端 `tasks.py` 新增任務留言 CRUD + 附件 CRUD（upload/get/delete/download）
- [x] 附件儲存於 `uploads/task_files/`，DB 保留 `original_filename`（解決 `secure_filename` 過濾中文問題）
- [x] 前端 `timelineService.js` / `taskService.js` 補完留言 + 附件方法
- [x] `TimelineDetailDialog.vue` 任務詳情 — 附件預覽/下載/刪除 + 留言新增/刪除
- [x] `TasksView.vue` 任務點擊 → 詳情 Modal — 附件 + 留言功能
- [x] 下載改用 fetch+blob（解決跨來源 `<a download>` 無效問題）
- [x] 重複路由整合：`timelines.py` 移除 6 條與 `tasks.py` 重複的路由（upload/getFiles/deleteFile/getComments/addComment/deleteComment）
- [x] `timelineService.js` 移除重複的留言/附件方法，統一由 `taskService` 處理
- [x] 所有 model 補上 `cascade='all, delete-orphan'`（TaskComment、TaskUser、Subtask、TaskFile、TimelineUser）

#### 垃圾桶機制 ✅ 完成（2026/03/02）
- [x] 後端新建 `trash.py` blueprint：`GET /api/trash`、`PATCH /restore`、`DELETE`（任務 + 專案各一套）
- [x] 永久刪除專案時同步清除所有子任務、附件（cascade + `os.remove`）
- [x] 前端 `trashService.js`（5 個方法）
- [x] `TrashView.vue`：已刪專案 + 已刪任務兩區塊，還原 / 永久刪除按鈕，非建立者唯讀
- [x] Router 新增 `/trash`、Sidebar 新增 🗑️ 垃圾桶入口
- [x] 修正：刪除專案後重新 call API（cascade 同步刪子任務，前端無法自行推算）

---

### Phase 1 收尾技術債（建議 Phase 2 開始前完成）

#### Store 層補完 ✅ 完成（2026/03/02）
- [x] `stores/timelines.js` — timelines/allTasks 狀態 + computed（urgentCount/sortedTimelines 等）+ 全部 CRUD
- [x] `stores/groups.js` — groups/messages/currentGroup + scrollCallback 機制
- [x] `stores/profile.js` — profile/stats/statCards computed
- [x] 三個 View 更新改用對應 store（TimelinesView / GroupsView / ProfileView）

#### Subtask 前端 UI 補完 ✅ 完成（2026/03/02）
- [x] `TasksView.vue` 任務詳情 Modal 顯示子任務（後端 + store 方法已備妥，可直接呼叫 `taskService.getSubtasks` 等）
- [x] `TimelineDetailDialog.vue` 任務詳情同步補上

---

### Phase 2 — 功能完善（已完成 ✅）

#### 通知系統 ✅ 完成（2026/03/06，目標 03/09）

**後端：**
- [x] `timelines.py`：`add_timeline_member` 邀請成員後自動發送 `timeline_invited` 通知
- [x] `timelines.py`：新增 `GET /api/timelines/upcoming` — 3 天內截止 OR 進度 ≥80% 的專案列表
- [x] `tasks.py`：新增 `GET /api/tasks/upcoming` — 3 天內截止 OR 進度 ≥80% 的任務列表（含自建 + 指派 + timeline 成員）

**前端：**
- [x] `Header.vue`：30 秒輪詢 `fetchUnreadCount()`（`setInterval` + `onUnmounted` 清除，避免洩漏）
- [x] `Header.vue`：`notifIcon` map 補上 `timeline_invited: '👥'`
- [x] `taskService.js`：新增 `upcoming()` 方法
- [x] `timelineService.js`：新增 `upcoming()` 方法
- [x] `HomeView.vue`：完整重寫 — 新增「⏰ 即將到期 / 進度落後」區塊，逾期（紅）/ 即將到期（琥珀）色彩標記，點擊導向對應路由

#### Timeline 任務成員指派 ✅ 完成（2026/03/06）
- [x] `TimelineDetailDialog.vue`：任務列表每行加 👥 按鈕（負責人 role=0 可見），開啟獨立指派 panel
- [x] `TimelineDetailDialog.vue`：任務詳情 Dialog 內嵌「指派成員」區塊 — 已指派標籤 + 快速指派按鈕
- [x] `openTaskDetail()` 同時載入所屬 timeline 成員列表，確保資料備妥

---

### Phase 2 — 進行中 / 待做

- [x] 全站 `alert()` / `confirm()` 換成 Toast / 確認 Modal（技術債）✅ 完成（2026/03/07）
- [x] 數據分析儀表板 ✅ 完成（2026/03/07）
- [x] 行動版 RWD 全站適配 ✅ 完成（2026/03/11）
- [x] CSS Layer Bug 修復（`style.css`）✅ 完成（2026/03/11）

---

### Phase 2 — 行動版 RWD ✅ 完成（2026/03/11）

#### 行動版 RWD 全站適配 ✅ 完成（2026/03/11）
- [x] `App.vue` — `isMobile` 偵測 + resize 監聽，手機單欄 Grid（`1fr`），`sidebarOpen` 預設 false
- [x] `Sidebar.vue` — 桌機側邊欄 `hidden md:block fixed`，手機底部導航 `md:hidden fixed bottom-0 z-[9999]`（五個頁籤）
- [x] `Header.vue` — 手機縮短 padding（`px-3 md:px-8`）、隱藏用戶名（`hidden md:block`）、精簡登出按鈕、通知面板寬度限制 `w-[min(20rem,calc(100vw-1rem))]`
- [x] 全部 7 個 View — 最外層加 `pb-24 md:pb-6`，防止內容被底部導航遮住
- [x] 頁面標題 — `text-2xl md:text-4xl`、emoji `text-4xl md:text-6xl` 響應式縮放
- [x] `TimelineViewModes.vue` — 最外層加 `mb-20 md:mb-0`，撐開 Grid 行高，確保最後一張卡片可捲動出底部導航以上

#### CSS Layer Bug 修復 ✅ 完成（2026/03/11）
- [x] `src/style.css` 全域重置 `* { padding: 0 }` 未包進 `@layer base`，導致 Tailwind v4 所有 padding utility 被靜默覆蓋（`p-5` 加了等於沒加）
- [x] 修法：`* { ... }` 包進 `@layer base {}`，讓 `.p-*` specificity (0,1,0) 勝過 `*` (0,0,0)
- [x] 詳細記錄：`docs/CSS_Global_Reset_覆蓋_Tailwind_Padding_問題.md`

---

### Phase 3 — TypeScript 遷移 ✅ 完成（2026/03/15）
> **前提：JavaScript 重構完成且穩定運行後**  
> **原則：漸進式遷移，層級由底到上**  
> **狀態：全站 TypeScript 化完成 ✅**

#### 基礎層 ✅ 完成（2026/03/13）
- [x] `types/index.ts` — 定義共用型別（Timeline、Task、Todo、User、Group、Message、Notification、Profile、ChartStats、DaysRemainingResult 等）
- [x] TypeScript 5.9.3 安裝為 devDependency
- [x] `tsconfig.json` 配置（strict mode 啟用）

#### Service 層 ✅ 完成（2026/03/13）
- [x] `services/api.ts` — `RetryableAxiosRequestConfig` 擴充、`FailedQueueItem` 型別、`processQueue` 加型別
- [x] `services/socketService.ts`
- [x] `services/taskService.ts`
- [x] `services/timelineService.ts`
- [x] `services/todoService.ts`
- [x] `services/groupService.ts`
- [x] `services/profileService.ts`
- [x] `services/notificationService.ts`
- [x] `services/trashService.ts`

#### Store 層 ✅ 完成（2026/03/13）
- [x] `stores/auth.ts`（認證狀態 + token 持久化）
- [x] `stores/todos.ts`（CRUD + computed filters）
- [x] `stores/tasks.ts`（CRUD + subtask 操作）
- [x] `stores/timelines.ts`（日期計算 + CRUD + 成員管理）
- [x] `stores/groups.ts`（群組 + 訊息 + currentGroup）
- [x] `stores/profile.ts`（個人資料 + 統計計算 + 圖表資料）
- [x] `stores/notifications.ts`（通知 CRUD）

#### Composables ✅ 完成（2026/03/13）
- [x] `composables/useConfirm.ts` — Promise-based 確認對話框（含型別）

#### 元件層 ✅ 完成（2026/03/15）
- [x] 全站元件改為 `<script setup lang="ts">`
- [x] 所有元件補齊 Props / Emits 型別定義
- [x] Vue 3 `defineProps` / `defineEmits` 搭配 `<script setup>`

---

### Phase 4 — 進階功能 ✅ 完成（2026/03/18）
> **目標：前後端契約強化 + 甘特圖視覺化 + 後端分層重構 + WebSocket 落地**  
> **狀態：所有項目已完成並通過驗證**

#### 4.1 Payload Contract / Guardrails ✅ 完成（2026/03/15）
- [x] 新增 `utils/payloadMappers.ts` — 統一 Create/Update payload 輸出
- [x] Service mutation 回傳型別與後端契約對齊（message / id / task_id / completed / status）
- [x] 後端 create/update 端點採 allowlist + unknown field reject
- [x] 新增 guardrail 腳本：`npm run guardrails:payload`（靜態檢查）
- [x] 新增 `docs/payload-contracts.md` 作為端點契約文件
- [x] 驗證通過：`npm run guardrails:payload` + `npm run build` 成功

#### 4.2 甘特圖視覺化 ✅ 完成（2026/03/16）
- [x] 整合 `frappe-gantt` 1.2.2（輕量級，無外部依賴）
- [x] 任務拖曳回寫至 API（start_date / end_date）
- [x] Hover popup 顯示任務詳情
- [x] Label 截斷防止溢出
- [x] 修正 `custom_class` 空白 token 導致 DOMTokenList.add crash
- [x] 月曆日期改為 local date-only 解析（避免時區偏移錯日）
- [x] 整合於 `TimelineViewModes.vue` 甘特視圖

#### 4.3 後端 Service Layer 重構 ✅ 完成（2026/03/16）
- [x] 建立 `backend/services/` 並新增 9 個 service 檔案
- [x] `services/task_service.py` — 任務權限 decorator、allowlist validation
- [x] `services/timeline_service.py` — 專案權限 decorator、角色繼承邏輯
- [x] `services/todo_service.py` — Todo allowlist + serializer
- [x] `services/profile_service.py` — Profile allowlist + serializer
- [x] `services/group_service.py` — 群組 invite code + serializers
- [x] `services/notification_service.py` — 通知 serializer
- [x] `services/trash_service.py` — 垃圾桶 serializer + 檔案清除 helper
- [x] `services/auth_service.py` — auth/current user serializer
- [x] `services/message_service.py` — 未讀訊息 query helper
- [x] 所有 blueprint 改為引用對應 service（移除重複 helper）
- [x] 後端編譯檢查：`python -m compileall blueprints services models app.py` 通過

#### 4.4 新增 API 端點（任務主責設定）✅ 完成（2026/03/16）
- [x] `PATCH /tasks/<task_id>/members/<member_id>` — 設定任務主責
- [x] 權限修正：任務主責 / 任務建立者 / 專案主責皆可操作
- [x] 自動補建 `task_user` 若不存在

#### 4.5 WebSocket 即時通訊 ✅ 完成（2026/03/18）
- [x] **Slice A：後端 Socket 接線（2026/03/18）**
  - [x] Flask-SocketIO 事件定義（join/leave/send/broadcast）
  - [x] JWT 握手認證
  - [x] Group room 廣播隔離
  - [x] Thread-based 非同步處理
- [x] **Slice B：前端 Socket 疊加（2026/03/18）**
  - [x] Groups store 新增 Socket 事件監聽
  - [x] 訊息即時推送（socket → store 狀態更新）
  - [x] REST fallback（Socket 失敗時降級）
  - [x] 不破壞既有聊天流程
- [x] **Slice C：授權隔離與驗證（2026/03/18）**
  - [x] 跨群組隔離測試（成員無法看到其他群組訊息）
  - [x] 授權檢查（JWT token 驗證）
  - [x] 重連流程驗證
  - [x] 雙端即時測試完成
- [x] 後端 realtime 分層重整：`backend/realtime/socket_events.py`
- [x] `start_all.bat` 啟動修正（後端改由 `python app.py` 啟動 Socket server）

### Phase 5 — 上線與部署整合 ✅ 完成（2026/03/22）
> **目標：展示級生產環境上線，測試核心功能可靠性**  
> **方案：Railway + Supabase + Firebase，月費 $5-10**  
> **狀態：5.1~5.4A 完成，5.5 已建立測試基線，5.6 規劃完成**

#### 5.1 PostgreSQL 遷移 ✅ 完成（2026/03/22）
- [x] Supabase 免費專案建立
- [x] PostgreSQL 連線字串配置（`.env` DATABASE_URL）
- [x] 後端改用 psycopg2-binary driver（`requirements.txt` 已更新）
- [x] 本地 migration 至 Supabase — `init_db.py` 直接建立 15 個表格
- [x] 本地連線驗證 — PostgreSQL 連接成功且所有表格已建立
- [x] 流程文檔編寫：`docs/Phase5_1_PostgreSQL遷移流程.md`

#### 5.2 Railway 後端部署 ✅ 完成（2026/03/22）
- [x] Railway 帳號及專案建立
- [x] GitHub 倉庫綁定 + main 分支自動部署
- [x] 環境變數設定（DATABASE_URL、SECRET_KEY、JWT_SECRET_KEY、FLASK_ENV=production）
- [x] Flask 啟動改為支援 `PORT` 環變（Railway 動態分配）
- [x] Gunicorn + Eventlet 生產伺服器配置
- [x] 循環導入問題修復（4 波 commit 逐一解決）
- [x] 取得公網 URL：`prajekt-production.up.railway.app`
- [x] 健康檢查驗證：`GET /api/health → 200 OK` 成功
- [x] 資料庫初始化成功 — 15 個表格建立完成
- [x] WebSocket 連線驗證：`/socket.io` 握手 200
- [x] 流程文檔編寫：`docs/Phase5_2_Railway後端部署.md`

#### 5.3 前端配置 + Firebase 部署 ✅ 完成（2026/03/22）
- [x] 取得 Railway 公網 URL 並更新 `frontend/.env.production`
- [x] 本地 `npm run build` 驗證 — 編譯成功
- [x] Firebase 前端專案初始化（`prajekt-kiriris`）
- [x] Firebase Hosting 部署成功
- [x] 前端 URL：`https://prajekt-kiriris.web.app` ✅
- [x] 驗證 CORS 配置 — 後端允許 Firebase domain
- [x] Commit 0b0d6e0：添加 Firebase Hosting 域名到 CORS 允許列表
- [x] 三服務聯動驗證通過（Firebase + Railway + Supabase）
- [x] 流程文檔編寫：`docs/Phase5_3_Firebase前端部署.md`

#### 5.4A 端對端基本驗證 ✅ 完成（2026/03/22）
- [x] 單人核心流程確認（登入 → 建立任務 → 群組聊天 → 任務操作 → 待辦管理）
- [x] 生產環境健康檢查（`GET /api/health` 回應 200；`/socket.io` 握手 200）
- [x] GitHub README 更新（生產環境 URL、快速開始指南）
- [x] 驗收範圍註記：本輪上線不包含 AI 任務生成

**預期成果**：
- ✅ PrAjeKt 線上運行（Firebase 前端 + Railway 後端 + Supabase 資料庫）
- ✅ 月費成本：$5-10（僅 Railway 後端），無前端與資料庫額外費用
- ✅ 本輪驗收完成（單人核心流程），可用於展示
- ✅ 本輪上線範圍已明確排除 AI 任務生成

#### 5.5 前端測試框架 ✅ 完成（2026/03/25）
> **目標：建立全面的前端單元測試，保證核心功能可靠性**  
> **工具：Vitest 4.1.1 + Vue Test Utils + Pinia**  
> **成果：85 個測試全數通過，平均覆蓋率 88.85%**

**已完成測試範圍**
- [x] Services 層（8 檔案，24 tests，100% 平均覆蓋率）— API 端點映射驗證
- [x] Stores 層（7 檔案，35 tests，83.24% 平均）— 狀態管理與 computed 驗證
- [x] Utils 層（3 檔案，17 tests，97.02%）— formatters / validators / payloadMappers
- [x] Composables 層（1 檔案，4 tests，100%）— useConfirm 狀態與事件流

**測試品質評估**
| 指標 | 達成 |
|------|------|
| **Tests 通過率** | ✅ 85/85（100%）|
| **TypeScript 型別檢查** | ✅ 0 errors |
| **Average Statements** | ✅ 88.85% |
| **Branch Coverage** | ⚠️ 71.56%（邊界測試缺口）|
| **Untested** | ⚠️ Views / Components / Router |

#### 5.6 後端測試實作 ✅ 完成（2026/03/29）
> **目標：建立後端 API 路由與 business logic 的單元測試**  
> **工具：pytest + pytest-flask + SQLAlchemy test fixtures**  
> **狀態：主要 blueprint（auth/todos/tasks/timelines/groups/profile/notifications/trash/messages）+ core services 完成，並已接上 CI coverage 報告（term/xml/html）**

**建議測試覆蓋範圍**
- [x] `blueprints/auth.py` — login/register/refresh/logout 的流程 + 錯誤情況
- [x] `blueprints/tasks.py` — 第一波：auth guard、建立/查詢/刪除軟刪除主流程
- [x] `blueprints/timelines.py` — 第一波：建立/查詢/更新驗證/刪除連動軟刪除
- [x] `blueprints/todos.py` — CRUD + toggle 邏輯
- [x] `blueprints/groups.py` — 第一波：建立、加入、離開、訊息存取權限
- [x] `blueprints/profile.py` — get/update + search + chart-stats
- [x] `blueprints/notifications.py` — fetch / unread-count / mark-read / read-all / delete
- [x] `blueprints/trash.py` — restore / permanently delete + cascade 清理
- [x] `services/task_service.py` / `timeline_service.py` / `auth_service.py` / `notification_service.py` — 第一波核心 service 測試
- [x] `services/group_service.py` / `message_service.py` / `profile_service.py` / `todo_service.py` — serializer/helper/query 測試

## CI/CD（Side Project 輕量版）

### C1 | Pull Request 基本檢查（GitHub Actions）
- [ ] Frontend：`npm ci` + `npm run test:run` + `npm run build`
- [ ] Payload 契約守門：`npm run guardrails:payload`
- [x] Backend：GitHub Actions 已啟用 `pytest + coverage`（term/xml/html）

### C2 | Branch 保護策略（精簡）
- [ ] `main` required status checks（Frontend Build + Frontend Tests）
- [ ] 禁止直接 push `main`（改走 PR merge）

### C3 | 部署流程（維持現況）
- [x] 後端：Railway 由 `main` 自動部署
- [x] 前端：Firebase Hosting 既有部署流程可用
- [ ] 補一份 `docs/CI_CD_最小流程.md`（提交規範 + 發版步驟 + 回滾步驟）

---

## Phase 6 - AI 本地產品主線（2026/04 規劃開始）

> **前提：Phase 5 已完成上線；Phase 6 僅做本地執行與驗證，不新增雲端部署**
>
> **技術選型**：Dify（本地 Docker）+ LM Studio（OpenAI Compatible）+ Qwen3.5-9B + 本地 Flask API
>
> **主線策略**：先做三個高實用 RAG 功能，不再以「先湊 100 筆舊任務」作為前提
>
> ⚠️ **本地開發專用** —— Dify / API / 向量庫皆本地部署

### Phase 6 分階段清單

| 階段 | 內容 | 週期 | 狀態 |
|-----|------|------|------|
| 6.1 | 本地環境準備（Dify + LM Studio + Qwen3.5-9B） | Week 1-2 | ⏳ 待開始 |
| 6.2 | RAG-A：Task Comment 智能摘要（決議/風險/下一步） | Week 3-4 | ⏳ 待開始 |
| 6.3 | RAG-B：Group 知識快照（近 30 天訊息+評論） | Week 5-6 | ⏳ 待開始 |
| 6.4 | RAG-C：個人週回顧（本週完成/阻礙/下週重點） | Week 7-8 | ⏳ 待開始 |
| 6.5 | MCP 接線與文檔收斂（Workflow/Prompt/Runbook） | Week 9-10 | ⏳ 待開始 |

### 6.1 | 本地環境準備（Week 1-2）

**功能：無新功能，純環境配置**
```
- Dify Docker 本地啟動（含 PostgreSQL 持久化）
- LM Studio 本地 API 啟動（OpenAI compatible）
- Qwen3.5-9B Instruct 載入與推理驗證
- Flask Backend 連接設定
```

**待辦清單：**
- [ ] Docker 本地啟 Dify（含 PostgreSQL 持久化）
- [ ] 啟動 LM Studio 並開啟 API（`http://127.0.0.1:1234/v1`）
- [ ] 載入 Qwen3.5-9B（建議先 4-bit）
- [ ] Dify 模型供應商改指向 LM Studio
- [ ] 連接本地 Flask API（`http://localhost:5000`）
- [ ] 驗證端到端呼叫（Dify → LM Studio → Flask）

### 6.2 | RAG-A：Task Comment 智能摘要（Week 3-4）⭐ 核心

**前端功能**
```
TaskDetailModal 新增「AI 摘要」
├─ 一鍵整理任務留言與備註
├─ 回傳：決議 / 風險 / 下一步
└─ 結果可複製到 remark（不自動覆寫）
```

**後端功能**
```
新增 API endpoint
└─ POST /api/tasks/{task_id}/ai-comment-summary
   ├─ 來源：task + comments + subtasks 最近變更
   └─ 返回：{ decision, risks, next_actions, references }
```

**待辦清單：**
- [ ] 新增 `POST /api/tasks/{task_id}/ai-comment-summary`
- [ ] Dify Prompt v1（評論摘要模板）
- [ ] 前端 TaskDetailModal 接線
- [ ] 評測文件：`docs/phase6-rag-evals.md`（RAG-A 區段）

### 6.3 | RAG-B：Group 知識快照（Week 5-6）

**前端功能**
```
GroupsView 新增「AI 群組快照」
├─ 本週進展
├─ 目前卡點
├─ 重要待辦
└─ 來源連結（可點回原訊息）
```

**後端功能**
```
新增 API endpoint
└─ POST /api/groups/{group_id}/ai-snapshot
   ├─ 來源：近 30 天 messages + task comments
   └─ 返回：{ highlights, blockers, todos, references }
```

**待辦清單：**
- [ ] 新增 `POST /api/groups/{group_id}/ai-snapshot`
- [ ] 實作近 30 天資料抓取器
- [ ] 前端 GroupsView Snapshot 卡片
- [ ] 評測文件補 RAG-B 指標（準確度/可追溯性）

### 6.4 | RAG-C：個人週回顧（Week 7-8）

**前端功能**
```
ProfileView 新增「本週 AI 回顧」
├─ 本週完成事項
├─ 卡點與原因
├─ 下週重點
└─ 一鍵存檔與歷史回查
```

**後端功能**
```
新增 API endpoint
├─ POST /api/profile/ai-weekly-review
└─ GET  /api/profile/ai-weekly-reviews
```

**待辦清單：**
- [ ] 新增 `ai_weekly_reviews` 輕量資料表
- [ ] 新增建立/查詢週回顧 API
- [ ] 前端 ProfileView 回顧面板 + 歷史清單
- [ ] 評測文件補 RAG-C 實用性記錄

### 6.5 | MCP 接線與文檔收斂（Week 9-10）

**待辦清單：**
- [ ] 新建 `mcp_server.py`（封裝三個核心工具）
  - [ ] `task_comment_summary(task_id)`
  - [ ] `group_snapshot(group_id)`
  - [ ] `weekly_review(user_id, week)`
- [ ] Dify MCP Server 連接配置與工具發現驗證
- [ ] 文檔輸出
  - [ ] `docs/phase6-local-setup.md`
  - [ ] `docs/phase6-rag-evals.md`
  - [ ] `docs/phase6-mcp-tools.md`
  - [ ] `docs/workflows/`（Dify workflow 匯出）

**預期成果**
- 三個 RAG 功能可在本地穩定使用
- Qwen3.5-9B 能支撐日常開發調試
- MCP 為下一步擴展保留統一介面

---

## 📊 整體進度估算

> 基準：02/26 開始 Phase 1、不計入休息日、Vibe Coding 約 3x 加速

| Phase | 狀態 | Vibe Coding 預估 | 目標日期 |
|-------|------|------------------|---------|
| Phase 0 — 結構重構 | ✅ 完成 | — | — |
| Phase 1 — MVP 功能 | ✅ 完成（提前） | Vibe ~1 週 | **03/02** |
| Phase 2 — 功能完善 | ✅ 完成（提前） | Vibe ~1 週 | **03/11** |
| Phase 3 — TypeScript | ✅ 完成 | 全站完成 TS 化 | 2026/03/15 |
| Phase 4 — 進階 | ✅ 已完成 | Payload Contract / Guardrails + 甘特圖 + 後端 Service Layer + WebSocket Slice A/B/C + realtime 分層重構 | PostgreSQL 併入 Phase 5 |
| Phase 5 — 上線部署 | ✅ 已完成（5.6） | PostgreSQL + 部署整合 + 前後端測試基線建立 + 後端 CI coverage 報告 | 2026/03/29 |
| CI/CD（輕量版） | 🟡 進行中 | Backend PR checks + coverage 報告已完成；待補 Frontend checks/branch 保護 | 2026/04 |
| Phase 6 — AI 本地主線 | 🟡 規劃中 | Dify + LM Studio + Qwen3.5-9B + 三個 RAG + MCP（本地驗證） | 2026/04 |

---

## 快速時間軸

| 日期 | 要完成的小項 | 狀態 |
|---------|----------|------|
| 02/26 | 多人協作後端（decorator + ORM 全面改寫）| ✅ 完成 |
| 02/26 | 前端 role-based UI 按鈕隱藏 | ✅ 完成 |
| 02/27 | 舊資料 backfill + 成員管理 UI + tasks.py 安全修補 | ✅ 完成 |
| 02/27 | 前端任務多人協作 UI（取代 assistant 欄位）| ✅ 完成（超前） |
| 02/28 | 前端任務多人協作 UI（取代 assistant 欄位）| ✅ 提前 |
| 03/01 🎯 | 多人協作全部收尾 | ✅ 提前至 02/27 |
| 03/01 | 任務留言 + 檔案上傳 後端 + 前端 | ✅ 完成 |
| 03/02 | 垃圾桶機制：API + 頁面 + 修正 cascade | ✅ 完成 |
| 03/02 | Store 層補完（timelines / groups / profile）+ View 更新 | ✅ 完成 |
| 03/02 | Subtask 前端 UI（TasksView + TimelineDetailDialog）| ✅ 完成 |
| 03/05 🌟 | **Phase 1 完成目標** | ✅ 提前完成（03/02） |
| 03/06 | 通知系統：專案邀請通知 + Header 30s 輪詢 + HomeView 即將到期區塊 + Timeline 任務指派 | ✅ 完成 |
| 03/07 | 全站 confirm() 換 ConfirmDialog（useConfirm composable + Headless UI）| ✅ 完成 |
| 03/07 | 數據分析儀表板：個人圖表（趨勢/狀態/專案量）+ 專案成員貢獻（ProfileView 整合）| ✅ 完成 |
| 03/09 🌟 | **通知系統完成目標** | ✅ 提前完成（03/06） |
| 03/11 | 行動版 RWD 全站適配（單欄 Grid / 底部導航 / Header 收縮 / 全 View pb-24 / TimelineViewModes mb-20）| ✅ 完成 |
| 03/11 | 修復 `style.css` CSS Layer bug：`* { padding: 0 }` 覆蓋全站 Tailwind padding | ✅ 完成 |
| 03/12 | 移除 `TimelinesView.vue` 死碼 `getDaysRemaining` + 更新 TS 遷移計畫 | ✅ 完成 |
| 03/12 | 數據分析儀表板 完成目標 | ✅ 提前完成（03/07） |
| 03/13 | Phase 3：基礎層 + Service + Store + Composables TS 遷移完成 | ✅ 完成 |
| 03/13 | 修正後端 UTC 時區輸出（`isoformat()` 補 `Z`）避免前端顯示提前 8 小時 | ✅ 完成 |
| 03/14 🌟 | **Phase 2 完成目標** | ✅ 提前完成（03/11） |
| 03/15 | Phase 3 元件層 TS 遷移完成 + Payload Mapper/Guardrails 落地（含 `npm run guardrails:payload`） | ✅ 完成 |
| 03/16 | Gantt（frappe-gantt）整合穩定 + 後端 Service Layer 全藍圖接線 + compile 驗證 | ✅ 完成 |
| 03/16 | Service Layer 第二輪瘦身（tasks/timelines helper 下沉）+ PostgreSQL 延後到 Phase 5（與部署併行） | ✅ 完成 |
| 03/16 | Phase 5 決策：主線 A/B、C+ 轉 Labs 支線 | ✅ 完成 |
| 03/18 | WebSocket Slice A：後端 Socket 事件接線（join/leave/send/broadcast）| ✅ 完成 |
| 03/18 | WebSocket Slice B：前端 Groups Socket 疊加 + fallback | ✅ 完成 |
| 03/18 | WebSocket Slice C：授權隔離/跨群測試/雙端即時驗證 | ✅ 完成 |
| 03/18 | 後端 realtime 分層重整（`backend/realtime/socket_events.py`）與 `start_all.bat` 啟動修正 | ✅ 完成 |
| 03/22 | Phase 5.1 資料庫遷移（SQLite → Supabase PostgreSQL）✅ 完成 | ✅ 完成 |
| 03/22 | Phase 5.2 Railway 後端部署 ✅ 完成（Gunicorn + Eventlet + 循環導入修復）| ✅ 完成 |
| 03/22 | Phase 5.3 Firebase + CORS 生產部署完成（前後端連通） | ✅ 完成 |
| 03/22 | Phase 5.4A 驗收收斂（單人核心流程）+ README 同步 + 明確排除 AI 任務生成 | ✅ 完成 |
| 03/25 | Phase 5.5 前端測試框架完成（Vitest 85 tests, 88.85% coverage）| ✅ 完成 |
| 03/26 | 技術棧全面更新（前端+後端+測試工具）+ .gitignore 補充 + 進度追蹤同步 | ✅ 完成 |
| 03/26 | Phase 6.1 Stryker 變異測試框架安裝與初次驗證（353 utils mutants）| ✅ 完成 |
| 03/29 | Phase 5.6 後端測試完成（pytest `108 passed`, coverage 80.59%）+ Backend CI coverage 報告上線 | ✅ 完成 |
| 04/01 | Phase 6 規劃改版：LM Studio + Qwen3.5-9B，主線改為三個高實用 RAG 功能（Task 評論摘要 / Group 快照 / 個人週回顧） | ✅ 完成 |

---

## 📝 Vibe Coding 速率參考

| 工作項目 | 原估 | 實際 |
|----------|------|------|
| Step 0-1 Service 層（5 個 service + 5 個 view）| 3 天 | 1 天 |
| 雜項 bug 修正 | — | 夾帶完成 |
| Step 0-2 Store 層（todos + tasks）| 3~5 天 | 1 天 |
| Step 0-3 TimelinesView 拆分（2569 → 301 行）| 1~2 天 | 1 天 |
| Phase 1 多人協作後端（decorator + ORM 全改）| 2~3 天 | 1 天 |
| Phase 1 前端 role-based UI | 1~2 天 | 半天 |
| Phase 1 tasks 安全修補 + 成員面板 UI | 1 天 | 半天 |
| Phase 1 任務留言 + 檔案上傳（後端 + 前端）| 2~3 天 | 1 天 |
| Phase 1 垃圾桶機制（API + 頁面 + bug fix）| 1~2 天 | 半天 |
| Phase 1 收尾技術債（Store 補完 + Subtask UI）| 1~2 天 | 半天 |
| Phase 4.11 WebSocket（Slice A/B/C + 分層重整）| 3~4 天 | 1 天 |
---

## 🚧 已知問題 / 技術債
- `formatDate()` 等工具函式仍有少量重複呼叫方式，後續可再收斂到 `utils/formatters.ts` 與 composable
- `TimelineViewModes.vue` / `TimelineDetailDialog.vue` 仍偏大，可於 Phase 5 規劃時納入第三輪細拆